### 2026-09-01 14:15 — snap
- Created research synthesis `content/research-paper-codebase-architecture-visualization.md` (hierarchical visual framework: paradigm-scale fit, semantic zooming, OOP+FP anti-pattern signatures, extraction pipeline) from a downloaded source doc whose formula images were corrupted — unverifiable thresholds hedged explicitly.
- Created **codebase-cartography** progressive-disclosure skill (`content/distilled/codebase-cartography/` + source prompt): 3 zoom levels (macro/meso/micro), text-based maps (DSM matrices, Mermaid, call chains), 7 anti-pattern health-check proxies; complementary to research-codebase (documentarian). Registered in manifest, essentials bundle.
- Two rule-of-5-universal reviews run and applied (research doc v1.0.1: hedged thresholds, expanded metric abbreviations; skill: 11 fixes incl. trigger ordering, layer-inference fallback, quoting-safe scans).
- Committed in 2 deliberate commits (c1cbe66, c747a16), pushed to origin/main. bd issue filed for pre-existing lefthook check-links false positives (59 in old research-*.md files). Untracked pre-existing files (other-skills/, reviews/, README.md mods) deliberately left untouched.
- **Next:** status is draft — test cartography on a real repo (calibrate health-check rg proxies/thresholds), then bump draft→tested. npm publish rides the next v* tag (v0.5.0). Consider fixing the link-checker bold-list misparsing before it annoys more commits.

### 2026-09-15 17:13 — snap
- Token-reduction plan executed end-to-end: 36 skills consolidated into 7 routers (session/review/planning/issues/diagnostician/prompt-meta/documentation), always-on cost 5,900 → ~3,100 tok (−47%); installer bugs fixed (double frontmatter, no prune, missing flag); selective desc trims; doc-link-verifier split
- skills.sh distribution added: committed `skills/` catalog via generate-skills-dir.mjs with `metadata.internal` for pointers; verified live (`npx skills add charly-vibes/incitaciones --list` = 38); GitHub Pages site + llms.txt now list the skills.sh path
- Open beads: incitaciones-8pv (npm 0.10.0 release + reinstall — shipping vehicle), ajp (monitor router triggers), mtw (remove pointers next major), 0tf, 22y
- **Next:** cut the 0.10.0 release (8pv) so installed copies in other repos pick up routers; watch trigger behavior in live sessions
