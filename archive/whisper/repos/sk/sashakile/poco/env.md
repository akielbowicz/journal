# poco — environment facts

### OpenSpec workflow facts (2026-09-20)
Specs-first repo: `poco/` is 15 lines of stubs; the architecture is 13 capabilities / 128 requirements / 386 scenarios across 5 pending changes (`add-core-architecture`, `add-advanced-data-integrity`, `fix-core-architecture-spec-errors`, `add-backend-adapter-protocol`, `add-provenance-tracking`); nothing archived yet, so `openspec/specs/` is empty and cross-change deltas reference each other's pending text.

`openspec validate --strict` gotchas (all hit this session):
- Requirement headers must be exactly `### Requirement: ...` — a `[TAG]` prefix before "Requirement:" is not parsed (project convention puts traceability tags in headers; base specs pass only because each section also has untagged requirements).
- SHALL/MUST is checked against the **first physical line of the requirement body only** — a keyword on a wrapped continuation line fails; also new requirements split out of an existing one need their own first-line SHALL.
- Two pending changes MODIFIED-ing the same requirement with full-text rewrites collide silently (second apply erases the first's addition). Fix pattern: declare an application order, rebase the later delta onto the earlier change's output text, and document it in both proposals.
- A delta requirement can target the wrong capability file (backend's pipeline delta carried dataframe's `explain()` requirement) — validator passes; catch it by cross-checking requirement names against base specs (`grep -h '^### Requirement:' .../specs/*/spec.md | sort | uniq -d` after filtering expected base+delta pairs).

Design decisions locked in (2026-09-20): fiber retention unconditional — no `keep_fibers` flag; bare `po.groupby(*keys)` → terminal `Fibers`, aggregating form → `DataFrame` exposing `.fibers`; `distinct` provenance = Injection (representative row, zero storage); `Selection` revisions = per-source monotonic counter assigned by every Selection-producing op; application order backend → provenance.

Architectural maps live in `docs/cartography/` (17 files: macro + 13 meso + 2 micro + index), regenerated by the codebase-cartography skill. Known archival seams: `distinct` touched by 3 changes; `diff()` specified in integrity but extends expression; stable-sort guarantee lives in integrity but governs pipeline; 3 deliberate co-ownership reference cycles (pipeline↔backend, provenance↔relational, provenance↔reducer).
