# Configuración de ZK para journal
[note]
dir = "."
filename = "{{slug title}}"
default-template = "default.org"
lang = "es"
id-charset = "alphanum"
id-length = 8
id-case = "lower"

[format.org]
extension = "org"
title = "#+title:"
link-format = "[[{{path}}][{{title}}]]"

# Aliases para crear notas rápidamente
[alias]
jrn = 'zk new --no-input jornal'
lab = 'zk new --no-input laboratorio --title "$@"'

[group.jrn]
paths = ["jornal"]

[group.jrn.note]
filename = "{{format-date now \"%Y-%m-%d\"}}"
template = "jrn.org"

[group.lab]
paths = ["laboratorio"]

[group.lab.note]
filename = "{{slug title}}-{{format-date now \"%Y-%m-%d\"}}"
template = "lab.org"
