# 2026-09-03

## Log

### Session:17:12 (pi + OpenRouter free models investigation)
- Investigated 21 free models available via OpenRouter API (`:free` suffix)
- Analyzed pi's session usage patterns: avg 95 LLM requests / 112 tool calls per coding task across 846 sessions
- Confirmed OpenRouter rate limits: 20 RPM, 50 RPD (<$10 purchased) or 1000 RPD (≥$10 purchased) — account qualifies for 1000/day
- Identified best coding free models: `cohere/north-mini-code:free`, `poolside/laguna-s-2.1:free`, `nvidia/nemotron-3-ultra-550b-a55b:free`, `nvidia/nemotron-3-super-120b-a12b:free`
- Documented how to create free-models-only API key via web UI (limit: $0) and via Management API
- Tested pi's built-in OpenRouter support — OAuth login works, API key already stored in auth.json
- Configured `modelOverrides` in `models.json` suggested for proper metadata
- **Next:** User may want to create a dedicated `limit: $0` key and test AFK tasks with free models
### Session:17:51 (el-mundo-ha-vivido-equivocado — timer stream + EPUB/PDF pandoc)
- Verificada grabación de hoy: timer systemd user `download-stream.timer` (jue 18:45, 1h30m) estaba trabado (`Trigger: n/a`) → `reenable + restart` lo arregló; próximo disparo hoy 18:45 ✓
- Nuevo `just check-stream-timer` + `scripts/check-stream-timer`: detecta timer sin próximo trigger y lo resetea; doc en `docs/workflow-programa.org` + tabla AGENTS.md
- EPUB: `build-epub.mjs` migrado de `marked` + `epub-gen` a **pandoc directo** (adiós dep `epub-gen`). Output limpio: título + contenido, sin bloque `.epub-meta` (género/licencia/fecha)
- PDF grillas: `build-show.mjs` ahora usa `scripts/print-style.css` dedicado (antes `resources/style.css` metía header/nav/breadcrumb/live-banner/borders del sitio). `--standalone` para conservar título; autor/fecha ocultos
- CSS de build movidos a `scripts/` (squint copia `resources/*.css` a `dist/` — habría contaminado el site)
- CI (deploy.yml): instala `pandoc` + `weasyprint` → EPUB y PDF se generan en el deploy; site solo sirve estáticos. Downloads links sin cambios (mismos paths)
- Commits pusheados: `8415269` (check-stream-timer) + `d7c1f81` (pandoc EPUB/PDF + CI). Pre-push hook OK (build, validate-built, a11y-audit)
- Verificado: EPUBs válidos (epubcheck 0 errores), 103 tests OK, links de descarga → existen
- **Next:** confirmar que el CI run post-push pasa el install de pandoc/weasyprint (push ~17:50); verificar grabación ~19:00 (`just check-stream-timer`, archivo en `materiales/grabaciones/`)
