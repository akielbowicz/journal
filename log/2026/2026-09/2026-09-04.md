# 2026-09-04

## Log

### Session:12:55 (XAct.jl — fix failing CompatHelper CI)
- Diagnosed daily CI failure: only CompatHelper workflow failing (since Sep 1), all other workflows green
- Root cause: `juliaregistries/compathelper` action repo was **deleted upstream** (404, not archived) → "Unable to resolve action" at ~7s every run
- Checked upstream: CompatHelper.jl is deprecated ("maintenance mode — migrate to Dependabot", which now supports Julia natively)
- Verified XAct.jl is a clean General-registry package (only non-General manifest entry is the normal `SxAct` self-reference) → safe for Dependabot
- Fix (commit `f5fd1a8`, pushed):
  - Deleted `.github/workflows/CompatHelper.yml`
  - Added `julia` package ecosystem to `.github/dependabot.yml` (weekly, `dependencies`/`julia` labels, `deps:` prefix) matching existing github-actions/pip entries
- Committed with explicit file paths (per AGENTS.md rule), `git push` succeeded, branch up to date with origin
- **Next:** confirm Dependabot opens Julia update PRs on next scheduled run; caveat — Dependabot doesn't support custom/private registries yet, revisit if one is added
