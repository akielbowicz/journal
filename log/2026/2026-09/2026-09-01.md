# 2026-09-01

## Log

### Session: 15:31 (incitaciones — cartography HTML export, v0.6.0 release)
- **Repo**: `~/para/areas/dev/gh/charly/incitaciones`
- Investigated obra/superpowers skills for HTML report generation: only the `brainstorming` visual-companion (zero-dep localhost server, HTML fragments + click events) does it; old `skills/architecture/` (deleted) had nothing HTML. Compared against `codebase-cartography`.
- Ran rule-of-5-universal on the proposal; converged on **static single-file HTML export** over the server protocol (no JS/CDN/server, markdown stays source of truth, reports must stay manipulable/diff-able).
- Implemented in `codebase-cartography` (commit d2c6c61): Optional HTML Export section in report-template.md, SKILL.md on-demand loading + derived-output rule, source prompt v1.1.0, manifest `html export` alias/hints.
- Second rule-of-5 pass on the change set caught filename collision (cross-level exports overwrite) + missing save-location default → fix 4231053: `cartography-<level>-<target>.html`, default `docs/`.
- Released **incitaciones@0.6.0** (minor): CHANGELOG `[0.6.0]` heading, `npm version minor`, tag pushed (had to recreate — the known pull-rebase tag-pruning quirk, now confirmed without any remote deletion), CI publish success, `pi install` verified `installed-version: npm:0.6.0`.
- **Next:** deferred items not built — extracting visual-companion as standalone interactive skill (beads issue not yet filed); incitaciones-0tf sync; `bd dolt push` remote config
### Session: 15:00 (incitaciones — agentic ticket research, skill updates, v0.5.0 release)

- **Repo**: `~/para/areas/dev/gh/charly/incitaciones`
- Created `content/research-paper-agentic-ticket-requirements-engineering.md` from a
  downloaded paper (Verifiable Value Claims: Planguage 8-param model, BDD/DbC/SDD/HDD
  verification matrix, F2P/P2P harnesses). Reviewed with rule-of-5-universal.
- Operationalized the research into two skills (v0.5.0):
  - `create-issues` v1.3.0 — issue template gains Verifiable Value Claim block
    (quantified Must gate, runnable Meter command, Baseline, regression suite) +
    Anti-goals; `base_commit` staleness anchor in beads metadata
  - `issue-review` — Pass 0 PRE-003/PRE-004 staleness checks (diff base_commit..HEAD
    vs metadata.files); Pass 5 verifiability watch-fors
- Dog-fooded the convention: filed incitaciones-0tf with `base_commit` metadata;
  empirically verified `bd update --metadata` merges keys (no data loss)
- Housekeeping: rewrote `just check-links` (old sed-range parser → 88 false
  positives; new frontmatter-aware python parser; 3 real broken links fixed),
  fixed 6 missing-`related` validator warnings, .gitignore for build artifacts +
  local working files → all gates zero-warning
- Published **incitaciones@0.5.0** to npm (CI success). Quirk: `npm version minor`
  created the commit but NOT the tag — had to `git tag -a v0.5.0` manually
- README mystery: working tree had the whole README replaced by `# archive` —
  created 14:49 today outside any script/commit (likely an earlier session
  testing /archive); discarded, README confirmed current, example versions updated
- Verified published package installs: `npx incitaciones@0.5.0 install` → 67
  skills, new value-claim content confirmed present in installed skills
- **Next:** incitaciones-0tf (sync 804-line issue-tracker-review source prompt);
  configure a `bd dolt push` remote; watch for the npm-version-tag quirk recurring

### Session: 15:52 (whisper repo-key dedupe — skill v0.7.0 + full migration)
- Root-caused `~/.whisper/repos/` duplication: the sed chain in all 5 whisper reference modes never handled `:` in ssh-alias remotes, and no canonical key was defined — sessions invented keys (bare names, owner dirs, `cv:charly-vibes/`, `gh/`, `.git` suffixes)
- **incitaciones@0.7.0**: whisper skill gains canonical repo-key rule (`host/owner/repo` from origin remote), "one repo, one key" routing rule, and new `/w consolidate` mode (approval-gated, append-merge with provenance headings) — commit 96e39d2, released & installed
- Executed consolidation on live data with user approval: 17 legacy keys → 4 canonical trees (`ak/`, `cv/charly-vibes/`, `github.com/`, `sk/sashakile/`); 22 envs append-merged with `## Migrated from <old-key>` headings, 1 branch-slot conflict merged (sk:titi), `genesis.git/` merged into `genesis/`; backup at `/tmp/whisper-backup`
- **Next:** nothing pending on whisper; incitaciones-0tf sync + `bd dolt push` remote still open

### Session: 16:01 (incitaciones v0.6.0/v0.7.0 releases + whisper dedupe)
- **Repo**: `~/para/areas/dev/gh/charly/incitaciones`
- Rule-of-5 on the cartography HTML export change set → fix 4231053 (level-scoped filenames `cartography-<level>-<target>.html`, `docs/` default save location)
- Released **incitaciones@0.6.0** (HTML export feature), CI publish + install verified
- Root-caused whisper repos duplication: reference sed chains never handled `:` in ssh-alias remotes, no canonical key defined → sessions invented keys (bare names, owner dirs, `cv:charly-vibes/`, `gh/`, `genesis.git/`)
- **incitaciones@0.7.0**: whisper skill v1.1.0 — canonical repo key rule (`host/owner/repo` from origin remote, `local/<repo>` fallback), "one repo, one key" routing rule, new `/w consolidate` mode (approval-gated append-merge with provenance headings). Commit 96e39d2, released & installed
- Executed live consolidation with user approval: 17 legacy keys → 4 canonical trees (`ak/`, `cv/charly-vibes/`, `github.com/`, `sk/sashakile/`); 22 envs merged with `## Migrated from` headings; conflicts merged (sk:titi main slot, genesis.git/); backup at `/tmp/whisper-backup`
- **Next:** spot-check merged envs then delete `/tmp/whisper-backup`; incitaciones-0tf sync; `bd dolt push` remote; journal repo push destination still missing

## Notes
- `bd update --metadata '{...}'` merges JSON keys rather than replacing — safe for
  partial updates (verified on incitaciones-0tf)
- `npm version` tag-creation failed silently (commit made, tag missing) — after
  `npm version`, always check `git tag -l` before pushing
- `.whisper/repos/charly-vibes/env.md` holds org-level env facts for the
  dulce-de-leche tool family
