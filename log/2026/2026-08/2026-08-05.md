# 2026-08-05

## Log

### Session:14:50 (vampiro — 23 tickets closed, v0.4.0 released)

- **Repo**: `~/para/areas/dev/gh/charly/vampiro`
- **Closed 23 tickets**: 3 P0s, 10 P1s, 5 P2s, 5 P3s — all open issues resolved
- **P0s**: CI job (build/test/clippy/fmt), CLI build deps (vendored openssl), behavioral guardrails
- **P1s**: genesis v0.6.0 envelope API, non-Rust frontend CLI tracers (Python/Clojure/Julia), 3 README fixes, shallow-git test, malformed YAML
- **P2s**: 3 CLI bugs (init-ci registry, prove no-op, --sarif flag), dead scan_threads config, doctor schema validation, path traversal fix
- **P3s**: aix/llms cleanup, README benchmarks, doctor test format, orphaned workspace members, rustfmt
- **Decision**: removed scan_threads field (was never wired into scan code)
- **Decision**: added commit SHA validation (hex-only 7-64 chars) for SnapshotStore
- **Released v0.4.0**: bumped Cargo.toml, CHANGELOG, tagged and pushed, release workflow succeeded (all 4 jobs)
- **Test count**: workspace tests pass, clippy clean, fmt clean
- **Remaining (manual)**: rotate CARGO_REGISTRY_TOKEN and TAP_GITHUB_TOKEN — requires human web actions

### Session:13:41 (dulce-de-leche — 14 tickets closed, v0.3.0 released)

- **Closed 14 tickets**: DDL-02a (--verbose impl), DDL-1n7/4xc/8z4/kl3/nj1 (33 new integration tests), DDL-786 (migrate tests verified), DDL-cy9/pdq/8xa/b1o/cj7/cvr (docs drift cleanup), DDL-6np (epic)
- **Implemented --verbose**: `verbose_print()` helper, all 8 installer functions now emit traces to stderr
- **Added 33 integration tests**: doctor (4), install (10), scope (6), status (5), version (7), plus 1 unit test for verbose_print
- **Fixed docs drift**: design.md/CLAUDE.md status updated, symlink direction corrected, exit codes fixed, verbosity levels documented, --profile minimal footnoted
- **Released v0.3.0**: bumped Cargo.toml, created CHANGELOG.md, tagged and pushed to origin/main
- **Test count**: 38 → 71 (33 new, all passing)
- **Quality gates**: clippy clean, fmt clean, all pushed
- **Decision**: --profile minimal is a reserved future feature, not implemented
- **Next**: any open DDL tickets from `bd ready` (currently 0 open)

### Session:15:49 (espectacular — 16 tickets closed, v0.5.0 released)

- **Repo**: `~/para/areas/dev/gh/charly/espectacular`
- **Closed 16 tickets**: 2 stale closes (026, tw0), 11 code/docs fixes, 1 feature, 1 decision, 1 release
- **Code changes**:
  - `espectacular-rmn` — removed local DoctorReport/DoctorDiagnostic/DoctorRecommendation types, use genesis::doctor
  - `espectacular-9ys` — adopted genesis v0.6.0 envelope API (cli_version caller-supplied)
  - `espectacular-b0v` — added quality-composability, quality-cost, quality-boundary-coverage signals
  - `espectacular-b19` — config path traversal validation
  - `espectacular-53j` — ah init stubs use CARGO_PKG_VERSION instead of hardcoded 0.1.0
  - `espectacular-i8q` — added about annotations to all ah --help subcommands
  - `espectacular-qg3` — fixed 3 pre-existing clippy warnings
- **Docs fixes**: README binary claim, commands table, --run-tests flag, AGENTS.md version string
- **Release v0.5.0**: bumped Cargo.toml + CHANGELOG, tagged, pushed
  - First attempt failed: Windows build had Unix-only code (killpg, process_group, PermissionsExt)
  - Fixed: gated all Unix-specific code in runner.rs behind `#[cfg(unix)]`
  - Second attempt: ✅ Linux build, ✅ macOS build, ✅ Windows build, ✅ GitHub Release, ✅ crates.io publish
  - ❌ Homebrew tap update failed (TAP_GITHUB_TOKEN not set in this fork)
- **Test count**: 303 tests pass, clippy clean, fmt clean
- **Remaining**: 2 tickets — espectacular-c9v (P0 pi extension), espectacular-djw (P2 mutation boundary)

### Session:17:08 (titi — 4 P1 tickets closed, v0.3.0/v0.3.1 released)

- **Repo**: `~/para/areas/dev/gh/sk/titi`
- **Closed 4 P1 tickets**: titi-mo7, titi-e59, titi-ark, titi-2uk
- **titi-mo7** (subprocess safety): routed restore and test-discovery call sites through `RunProcess` with concurrent async drain, bounded timeout, process-tree termination. Fixed 2 inline Process handlers that could deadlock or orphan dotnet children.
- **titi-e59** (confidence model): replaced `project-count/file-count` with weighted `ComputeConfidence(resolution×0.6 + freshness×0.25 + depth×0.15)`. Added `ResolvedFiles` to `AffectedSet`. Fixed false exit 10 on multi-file single-project changes.
- **titi-ark** (decision): recorded PackageReference identity policy — `PackageId` matching, semver-compatible version check, suppress cycles, skip unresolved externally.
- **titi-2uk** (graph): `GraphBuilder.Build` now resolves `PackageRefs` to Binary-mode edges. Added `IsVersionCompatible` helper with unit tests.
- **Chores**: removed unused ClojureCLR dependency, refreshed wai managed blocks, added `dont check` to pre-push hook, embedded README in NuGet package with AI notice.
- **Release v0.3.0/v0.3.1**: both published to NuGet.org via OIDC trusted publishing (NuGet/login@v1). CI workflow: build → test (433) → pack → OIDC login → push.
- **Test count**: 433 tests pass
- **Next**: no open issues remaining

### Session:17:21 (testaruda — 13 tickets closed, v0.4.0 released, ah-check restored)

- **Repo**: `~/para/areas/dev/gh/charly/testaruda`
- **Closed 13 tickets**: 1 P0, 6 P1s, 6 P2s — all open bugs resolved
- **P0s**: behavioral guardrails in AGENTS.md (already done, closed)
- **P1s**: titi fallback test always failing, garbled `--help` on subcommands, CI exit status lost in JSON mode, genesis v0.6.0 envelope API adoption, duplicate content units when symbol is NULL (schema migration v2→v4)
- **P2s**: CliFormat rustdoc in `--help`, command registry validate→oracle, default tracing INFO→warn, git porcelain rename `->` arrow parsing, ingest error missing valid outcome values, `explain` accepting human-readable node_ids
- **Quality**: 247 tests pass (was 113), clippy clean, fmt clean
- **Released v0.4.0**: bumped Cargo.toml, tagged, pushed. Release workflow triggered.
- **Decision**: duplicate content unit fix done as direct bugfix (per openspec "bug fixes restore intended behavior" skip-proposal rule), not openspec proposal despite hitl-approval label
- **ah-check pre-push gate restored**: bumped tool_version 0.2.2→0.3.0, fixed 6 invalid NF→NR archetypes, removed 5 orphan contracts, split duplicate scenario slug into Clojure/TypeScript variants, restored 2 accidentally removed scenarios, added test commands to 53 empty stubs, ran ah init
- **Next**: P3/P4 items remain (docs getting-started stale example, u32 overflow risk in Ascent distance, hand-rolled date formatter, engine scale-up, multi-repo composability)
