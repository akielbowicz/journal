# 2026-07-29

## Log

### Session:12:42 (el-mundo-ha-vivido-equivocado — extracted book index from WhatsApp photos)
- OCR'd 115 book cover/index photos from WhatsApp chat (43 books total) using tesseract + spa lang
- Built structured JSON (`resources/libros-index.json`) with title, author, cover/index photos, and parsed index entries (title + page number)
- Used Gemini 2.5 Flash (via OpenRouter API) to identify covers that tesseract couldn't read (artistic typography, low-res photos)
- Corrections applied iteratively with user: Alberto Laiseca (not Felisberto Hernández), Liliana Heker (not Liliana Colanzi), Alejandra Kamiya (3 books), Fabián Casas, Juan Forn, Roberto Arlt, Emmanuel Tornés Reyes, etc.
- Final: all 43 books identified, no uncertainties remaining
- Scripts and raw OCR in `extra/libros-ocr/`; final JSON in `resources/libros-index.json`
- Committed and pushed to origin/main (`851dceb`)
- **Next:** none — task complete

### Session:09:54 (titi AOT ticket + docs site + Ro5 review)
- **Closed titi-7ns** (AOT source-generated JsonSerializerContext): created `src/titi/Serialization/TitiJsonContext.cs` with all serialization types registered; replaced anonymous types in TestCli/Core/Adapter with named DTOs; added `[JsonPropertyName]` to SelectionLoader.JsonEdge for camelCase deserialization; added `PublishAot=true` to titi.csproj. All 219 tests pass, AOT publish succeeds (via `LIBRARY_PATH=/tmp/lib` workaround for missing brotli-dev on this host), binary runs `--help` exit 0. Adapter.cs kept reflection-based (subprocess, not AOT-compiled).
- **Created `add-docs-site` openspec proposal** (validated strict): DocFX v2.78.5 as dotnet tool, artifact-based GH Pages deployment (no gh-pages branch), `just docs` recipe copies openspec/specs → docs/specs/ at build time.
- **Implemented docs site**: `docs/docfx.json` + `docs/{index,cli,architecture,safety,adapter}.md` + `docs/toc.yml` + `docs/specs-toc.yml`; `.github/workflows/docs.yml` (upload-pages-artifact + deploy-pages); `just docs` recipe. Build: 0 warnings, 0 errors. Removed metadata/API-ref section — DocFX 2.78.5 can't compile .NET 10 source (`Cannot find .NET Core SDK`); conceptual docs only for now.
- **Fixed CI failure** (stale Release binary): `IntegrationTests.TitiOpen_AgainstFixture_GeneratesSlnx` failed because old Release binary didn't have updated `OpenCommandOutput` DTO. Added `NoWarn IL2026;IL3050;IL2104` to csproj (AOT analysis warnings only meaningful during publish, polluted stdout when `dotnet run` rebuilt inside tests). All 219 tests pass in Release.
- **Rule of 5 review on docs** (NEEDS_REWORK → fixed): 2 CRITICAL (fabricated `titi cache warm` command + `.titi/graph.cache` file that don't exist; phantom `Cli.cs` in source tree) + 6 HIGH (zero examples, stale status, wrong `symbol_model_complete` value). Fixed all: removed fabricated refs, regenerated source tree from `ls src/titi/*.cs`, added concrete examples (adapter handshake JSON, `titi open` output, REPL `tree` matching actual `PackageId  (path)` format), defined confidence factor formulas with actual code values, documented edge cases (malformed TRX no-overwrite, incremental record fingerprinting).
- **Commits pushed** (all on origin/main): `65d4edd` AOT, `0c76e01` docfx fix + NoWarn, `8bddedf` docs Ro5 fixes, `503b809` tree example fix.
- **Next:** (1) Enable GH Pages in repo Settings → Pages → Source: GitHub Actions, then docs deploy on next push. (2) DocFX API reference deferred until DocFX supports .NET 10 SDK compilation. (3) titi backlog empty — 0 open beads.


### Session:13:10 (vampiro-tmf.5 — edge-case corpus)
- Closed **vampiro-tmf.5** (Build edge-case corpus): created 23 edge-case source files across Rust (11), Python (4), Clojure (4), and Julia (4) — empty, comments-only, syntax errors, macros, attributes, generics, async, unsafe, enormous (1000 fn chain), Unicode, const eval, decorators, type-parameterized types
- Created `crates/vampiro-cli/tests/edge_case_tests.rs` — 23 tests asserting `vampiro check --full --mode guidance` runs without panic on each fixture
- All 750 workspace tests pass, committed as `bb9c19b`, pushed to origin/main
- **Next:** vampiro-tmf.3 (cross-language stress, P2) or vampiro-tmf.6 (benchmarks, P3)

### Session:13:51 (vampiro-tmf.3 — cross-language stress tests)
- Closed **vampiro-tmf.3** (Stress-test Python/Clojure/Julia frontends): created 8 realistic source fixtures across 3 languages (Python: HTTP lib, CLI, data pipeline; Clojure: HTTP server, async, data processing; Julia: data analysis, web server)
- Created `crates/vampiro-cli/tests/cross_language_stress_tests.rs` — 8 tests extracting CIR graphs from each fixture, validating extraction succeeds without panic
- Discovered & filed 4 frontend bugs discovered by testing:
  - vampiro-y4y (Python duplicate __init__ IDs, P3)
  - vampiro-276 (Julia duplicate anonymous function IDs, P3)
  - vampiro-3hk (Clojure edges to non-extracted macros/constructors, P3)
  - vampiro-fhg (Python edges to non-extracted builtins, P3)
- Added frontend crate dev-dependencies to CLI Cargo.toml
- All 758 tests pass, committed as `7ea9f53`, pushed to origin/main
- **Next (stress epic):** vampiro-tmf.6 — benchmarks (P3) — last remaining

### Session:15:05 (vampiro-tmf.6 — benchmarks)
- Closed **vampiro-tmf.6** (Benchmark large codebases): created `crates/vampiro-cli/tests/benchmarks.rs` with 4 benchmark tests (100, 1k, 10k, 50k lines) timing `vampiro check --full`
- Results: 100 lines = 10ms ✅, 1k = 271ms ✅, 10k = 21.3s ❌ (target <5s), 50k = 565s ❌ (target <30s), 100k = timed out
- Filed **vampiro-2li** (P2) for frontend performance investigation
- Wrote `docs/verification/benchmarks-1.md` with full results and recommendations
- 50k benchmark marked `#[ignore]` to avoid blocking CI
- **Closed stress-testing epic vampiro-tmf** — all 6 sub-tickets complete
- Committed `7f51587`, pushed to origin/main

### Session:16:02 (incitaciones docs-skills progressive-disclosure refactor)
- Read library-docs-best-practices research paper; performed gap analysis vs 3 existing documentation skills (research, implement, review) — found missing: LLM/agentic readiness (llms.txt, MCP, RAG), measurement (TTFS, coverage, freshness), platform comparison, versioning, anti-patterns catalog
- Applied Rule of 5 review (rule-of-5-universal) to own gap analysis — flagged provenance overclaim (5 refs cited frameworks.md for content it doesn't contain), fragile cross-skill path, report-template stage mismatch, header inconsistency, missing convergence bar
- Refactored 3 documentation skills from single-file to progressive-disclosure:
  - `research-documentation/`: SKILL.md + refs/{checklist,llm-readiness,metrics,report-template}.md
  - `implement-documentation/`: SKILL.md + refs/{templates,anti-patterns,tooling-comparison,versioning,diataxis-implementation}.md
  - `review-documentation/`: SKILL.md + refs/ai-readiness-criteria.md
- Fixed all 8 review findings: added research-finding-library-docs-best-practices.md as canonical source, replaced cross-skill ../ path with skill-name pointer, dropped report-template from Step 3, standardized Core Rules headers, added convergence bar + sibling cross-links, fixed provenance lines
- Published 0.3.1 to npm (patch), installed locally — discovered pi update does NOT re-sync flat-skills dirs
- Filed incitaciones-e3f (pi install update does not re-sync skills on version bump)
- Implemented fix in generate-pi-resources.mjs: reSyncFlatInstallSkills() detects flat-skills dirs with incitaciones markers and re-syncs them on postinstall
- Published 0.3.2 with fix, tested: removing skills from ~/.agents/skills/ + running generator restores them with npm:0.3.2 stamp
- **Next:** enable GH Pages in titi repo (Settings → Pages → Source: GH Actions) — docs deploy pending that one click

### Session:17:33 (wai — 6 tickets, release v2026.7.29, suite propagation)
- **Renew**: resumed session on wai repo; found wai-ak5q + wai-885g implemented but uncommitted (stranded work)
- **Landed stranded work**: committed + pushed wai-ak5q (pipeline-utilization doctor check) and wai-885g (Quick Start prefers `wai pipeline start`)
- **wai-knbl**: `wai prime` Pipelines section — active run (step N/M + next cmd), available list with phase-gated start suggestion. TDD: 3 tests.
- **wai-j56n**: inline doctor health summary in `wai status`/`wai prime` — silent when green, links to `wai doctor` on warnings.
  - Bonus: **managed-block always-stale bug fix** — `wai_block_content` had a spurious `\n` after `WAI:START` marker that genesis `BlockInjector` never writes, so every workspace falsely reported staleness. Fresh `wai init` is now doctor-green.
- **wai-hfgz**: pi `session_start` hook doctor check — scans `.pi/extensions/` for a handler running `wai prime`; gated on project-local `.pi/` for hermetic tests (avoids breaking clean-workspace contract on pi-using machines).
- **wai-pa3b**: `wai close` clears stale complete pipeline-run pointers (`.wai/.pipeline-run` + `.wai/resources/pipelines/.last-run`). Also gitignored `.last-run` (was accidentally tracked).
- **wai-bdqw.7**: Added "tracked with wai" badge to pretender README; ran `wai init` in pretender, espectacular, testaruda, dont, vampiro, crua, livin to refresh managed blocks. All pushed.
- **wai-bdqw epic**: closed — all 9 children complete.
- **Release v2026.7.29**: CHANGELOG entry, version bump in Cargo.toml, tag pushed (`v2026.7.29`) — GHA building release binaries, publishing to crates.io, updating Homebrew/Scoop.
- **Decisions**: pi check gated on project-local `.pi/` for hermetic tests; `.last-run` gitignored as runtime pointer like `.pipeline-run`.
- **Knowledge routed**: managed-block staleness fix & pi check gating decision noted in ~/.whisper/
- **Next:** wai-fvhv epic (QA round, P1, ~30 blocking-dependent tickets) — none ready, needs scope decisions first
