# 2026-07-30

## Log

### Session:14:00 (genesis v0.4.0 release)
- Added `Verbosity::from_verbose_count(u8)` and `CliVerbosity` args struct for `-v`/`-vv`/`-vvv` + `-q`/`--quiet` (pulled from wai's pattern)
- Added `OutputFormat` enum and `CliFormat` args struct with TTY auto-detect (JSON for agents/pipes, Human for terminals)
- Added `Output::emit()` and `Guide::run_formatted()` for format-dispatching output
- Added `Verbosity::help_footer()` for progressive-disclosure hint
- Created `discovery` module with `.genesis/tools.toml` manifest convention (scan, register, unregister, list_tools, has_manifest)
- Fixed flaky scratch tests (tempdir isolation, unique tool names per PID)
- Released genesis-vibes v0.4.0 (committed, tagged, pushed, published to crates.io)
- Bumped genesis-vibes dependency from `"0.2"`/`"0.3"` to `"0.4"` in all 6 tool repos (wai, dont, pretender, espectacular, testaruda, vampiro)
- Verified all 6 tool repos compile cleanly with v0.4.0 from crates.io
- Ran Rule of 5 review on the changeset; addressed all findings (raw_count(), doc fixes, scan return type)
- Updated existing adoption tickets (dont-4so7, pretender-042, espectacular-3ys) with v0.4.0 scope
- Created new adoption tickets: wai-4guc (P2), testaruda-a3p (P2), vampiro-1f3 (P3)
- Updated epic ticket genesis-vjp with full sub-ticket table
- **Next:** Adopt CliVerbosity/CliFormat in wai (wai-4guc) — replace raw `verbose: u8` and manual `--json` flag

### Session:15:00 (docs polish)
- Expanded README.md `guide` section with detailed explanation of `--json`/`--human` TTY auto-detect behavior
- Updated `llm.txt` and `llms.txt` to describe the format auto-detect: JSON for non-TTY stdout (agents/pipes), Human for interactive terminals
- Committed and pushed doc updates
- **Next:** Adopt CliVerbosity/CliFormat in wai (wai-4guc)

### Session:15:53 (epigraph quotes)
- Added epigraph quotes to README.md and docs homepage of all charly-vibes CLI tools, matching genesis's style:
  - **pretender:** Foo Fighters — "Send in your skeletons"
  - **livin:** Aerosmith — "Livin' on the Edge"
  - **crua:** Sumo — "Crua"
  - **vampiro:** Charly García — "Vampiro" (already in place)
  - **testaruda:** Shakira — "Se me acaba el argumento"
  - **dulce-de-leche:** Dicho popular — "Remando en dulce de leche"
  - **dont:** No Doubt — "Don't Speak"
  - **espectacular:** Peter de los Polvorines — "Porque mis armas son los pinceles"
- Each quote added to both README.md and docs homepage (index.md/introduction.md/getting-started.md)
- All 8 repos committed and pushed
- **Next:** wai and fotos still need quotes