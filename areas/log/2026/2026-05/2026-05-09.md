# 2026-05-09

## Focus
- miblioteca: debug tooling validation, wrap-up of multi-day feature sprint

## Log

### Sessions 13:47 (miblioteca — 14 sessions)
- **Bundle MVP (05-07):** Ran full Android Chrome verification of portable bundle export (ticket `u39`) — all passed; filed thumbnail bug `jwd` (thumbnails were full-res copies), fixed same session via `makeThumbnail`
- **UI polish (05-07):** Deferred backend tickets to 2026-07-01; implemented hash routing (`72k`), sessions list (`3fa`), captures gallery with URL lifecycle (`pa3`); fixed gallery race conditions via `gallerySeq` monotonic counter
- **Ghost overlay + capture metrics (05-07):** Scaffolded + ro5-reviewed OpenSpec for both; implemented all 8 code tickets — `rotateVec`, Welford laplacian, shift clamping, motion gating, null `grabFrame` guard, exposure threshold, `estimateDisplacement` — 438 tests green
- **App wiring + back button (05-08):** Fixed ghost overlay never activating (App.ts now wires real `Gyroscope`/`Accelerometer`); back button on `#/new`; bundle filename dedup fix; `overflow:hidden` clipping found and reverted
- **Debug event log (05-08, 4 sessions):** Proposed + ro5-reviewed `add-debug-event-log` OpenSpec; created 9 beads tickets; implemented `DebugLogger` (ring buffer), `DebugPanel`, `CaptureView` wiring, all 5 instrumentation tickets, 12 integration tests — spec fully complete, 484 tests green
- **Cross-browser compat (05-08):** `NotAllowedError` crash in Web Share fixed with download fallback; `DeviceMotionEvent` adapters for Firefox (deg/s→rad/s, axis mapping); added `simple-git-hooks` (pre-commit tsc, pre-push tests)
- **Ghost overlay scan axis fix (05-08):** Debug log confirmed `gz` (compass spin) was wrong axis; switched to `gy` (gamma) — reaches ±0.35 rad/s during scanning; added landscape `scanAxis` param; 498 tests
- **Translation tracking (05-08):** Integrated `DeviceMotion.acceleration` via trapezoidal rule with beta-angle guard and ZUPT on snapshot; fixed null-to-zero velocity drift; 542 tests; `add-distance-config` tickets created
- **Debug replay validation (05-09, today):** Configured Playwright MCP for HTTPS (`PLAYWRIGHT_MCP_IGNORE_HTTPS_ERRORS=true` env var); loaded `miblioteca-debug-2026-05-09` JSON into `debug-replay.html`; found and fixed `innerHTML +=` orphaning canvas elements in 4 places — zero console errors, all charts rendering, 566 tests green, pushed
- **Arc:** Project advanced from portable bundle MVP → UI completion → sensor-based ghost overlay → debug instrumentation → Firefox compat → translation tracking → debug tooling validated end-to-end
- **Next:** Manual device acceptance tests `mibilioteca-4n4` (ghost overlay on real device) and `mibilioteca-2tu` (bundle export on Android Chrome); implement `add-distance-config` starting with `mibilioteca-7st` (ruler calibration overlay)

### Morning notes
- Reading Hickey's "A History of Clojure" — languages as processable sets of design decisions (saved as poo)
- Nushell `reject` as complement of `select` (saved as TIL)
- Thought on AI interface trust: ask-first vs wall-of-text, trust builds over time via interaction memory (saved as poo)

## Tasks created

## Decisions
- `PLAYWRIGHT_MCP_IGNORE_HTTPS_ERRORS=true` added to global Claude Code `~/.claude/settings.json` env — needed for localhost HTTPS dev server validation
