# 2026-05-06

## Focus
- what the day is about

## Log

### Session 09:25 (hipervinculos)
- Investigated failed CI run on `hipervinculos` repo (GitHub Actions run #25410642165)
- Root cause: bookmark `a776ddc3` was missing its `title` field; URL was a Google bot-detection redirect instead of the real YouTube URL
- Fixed `data/bookmarks.jsonl`: added title "Full Workshop: Build Your Own Deep Research Agents" and corrected URL to `https://youtube.com/watch?v=mYSRn6PC1mc`
- Link check failures (Twitter 403, Colab 405, motifanalytics.com timeout, color-logic.io DNS) are transient/expected and non-blocking
- Committed fix as `2c43cd8` and pushed to `main`; CI should now pass

### Session 10:13 (REPLy.jl)
- Evaluated recent changes across code, docs, and tests
- Found `send` → `send!` naming bug in `docs/src/howto-mcp-adapter.md:140` — copy-pasteable example would raise `UndefVarError`
- Found docstring param mismatch in `src/mcp_adapter.jl:192`: `session_id_or_name` → `session_name`
- Found missing stub-tool test coverage for `julia_load_file`, `julia_interrupt`, `julia_complete`, `julia_lookup`
- Applied all three fixes in one commit (`REPLy_jl-bwm`); all 25 tests pass
- **Next:** 5 tracked P2 bugs remain open (audit dead code, lock contention, per-eval allocation, lifecycle duplication, Dict materialization)

### Session 11:01 (superficies)
- Diagnosed why the SaaS archetype was unresponsive to the hero layout control: the `ghost-dashboard` wrapper prevented the CSS selectors (targeting flat `.hero-zone > *` children) from matching
- Restructured `renderHeroByArchetype` for SaaS: flattened eyebrow/h2/copy/actions as direct `.hero-zone` children; renamed inner chrome wrapper to `.ghost-chrome`
- Added `data-archetype` attribute to `.hero-zone` in `render.ts` for CSS targeting
- Updated CSS: renamed `.ghost-dashboard` → `.ghost-chrome`, added per-layout placement rules for `.ghost-chrome` across all 5 layouts (hero+features, bento, magazine, sidebar+main, terminal)
- Verified all 5 layouts in browser — each produces a distinct visual composition for the SaaS archetype
- Committed and pushed as `feat(saas): make SaaS archetype responsive to hero layout selection`

### Sessions 12:51 (REPLy.jl — 2 sessions)
- **P2 sprint** (from workqueue): closed all 5 open P2 bugs — `AuditMiddleware` wraps requests and calls `record_audit!`, `TaskCapturingIO` replaces global `EVAL_IO_CAPTURE_LOCK` for concurrent IO capture, named-session stdin Pipe+feeder persists across evals, `receive()` returns `JSON3.Object` directly, `with_session_eval` extracted as shared helper; all 2060 tests pass; JET and Aqua clean
- **Resume session**: reviewed pending state — found stale beads issues `REPLy_jl-dxp` (IO capture lock) and `REPLy_jl-e30` (invokelatest barrier) still open despite being resolved by the sprint; closed both
- Closed the `ticket-workflow` pipeline; archived `holistic-review` project
- **Arc:** P2 sprint + cleanup → project is now in a clean baseline: no open issues, no active pipelines, no pending work
- **Next:** backlog review or new feature work (roadmap/openspec milestone)

### Sessions 14:24 (dont — 6 sessions)
- **Implementation sprint (sessions 1–5, 09:15–14:24):** init modes + prime, term definition + TermView, label validators (5 checks for `dont define --label`), lifecycle verbs (ignore, reopen, strict/permissive `conclude`), verify-evidence (per-reference liveness, partial-timeout, structural refusals), lock verb (lockable gates: 3+ hypotheses, 2+ evidence, no stale deps)
- Fixed dependency resolution bug during lock session: `--depends-on` CURIEs were being stored as raw strings instead of resolving to `term:ID`; added structured dismiss refusal coverage
- **Session 6 (14:24, current): QA pass on the dont CLI** — systematically exercised all subcommands; logged 12 bugs/UX issues (dont-1od through dont-sxh), covering: trust/dismiss internal error on terms, duplicate CURIE creation, prime ignored-count off-by-N, dependency enforcement not blocking dismiss, broken hint references, misleading optional flags, JSON-only output, missing list filters, CURIE lookup gaps, empty statement accepted
- **Issue review (/issue-review against specs):** 5-pass Ro5 across all 50 open issues; 18 findings actioned — split dont-736 (prime hint vs doctor remediation), added dep dont-bhx→dont-711, retyped dont-0um as spec violation with done criteria, marked dont-psu + dont-sxh HITL (design decisions needed), added spec refs + done criteria + dep links to all 9 QA issues, elevated dont-8cs to P1
- **Arc:** 5-session implementation sprint → full QA + issue-review hygiene pass; tracker is now clean with well-specified bugs and a clear dependency graph
- **Next:** dont-zal (P1 — CURIE uniqueness enforcement) or dont-1od (P1 — trust/dismiss dispatch on term entities)

### Session 15:27 (XAct.jl)
- Claimed and completed `sxAct-qozc`: added `test/julia/xtensor/test_textbook_gr_regressions.jl` and wired it into `test/julia/test_xtensor.jl`
- Added concrete GR regression checks for algebraic Riemann symmetries / first Bianchi, Schwarzschild sample Christoffel values + Kretschmann scalar, and FLRW spatial Ricci scalar
- Updated `README.md` and `docs/src/status.md` to point at the new textbook regression suite and clarify its relationship to the existing property/oracle layers (`sxAct-i2er`, `sxAct-96ry` remain the homes for second-Bianchi and randomized coverage)
- Renamed the suite from a JOSS-specific filename/reference to the neutral `test_textbook_gr_regressions.jl`; scrubbed JOSS/reviewer-facing wording from code comments and docs so publication context stays in tickets rather than implementation
- Verified with `just test-julia` and `just docs`; closed `sxAct-qozc`, committed `7af5700` (`Add textbook GR regression tests`), pushed `main`, and left the repo clean

### Sessions 16:03 (dont — 2 sessions)
- Kicked the tires on using `dont` against `../../sk/chacana/` and `../../sk/XAct.jl/` with isolated `DONT_DIR` stores under `/tmp`; confirmed the strongest current path is standalone claim grounding backed by repo evidence.
- Wrote research synthesis at `.wai/projects/dont/research/2026-05-06-dont-adoption-value-for-chacana-and-xactjl.md`, arguing `dont` is currently best as an epistemic sidecar rather than a full workflow spine.
- Authored and validated 3 OpenSpec proposals: `add-evidence-locators`, `add-ground-command`, and `add-trace-query`; then ran Rule-of-5 review on the proposals and fixed all spec-review findings (missing deltas, atomicity semantics, query/payload integration, edge cases).
- Created 7 beads tickets for the proposal set (`dont-2nh`, `dont-8bu`, `dont-tiv`, `dont-dxu`, `dont-7r9`, `dont-unm`, `dont-cw6`), wired dependencies, ran issue-review, and tightened the issue bodies (AFK/HITL markers, scope boundaries, validation commands, advisory sequencing ticket).
- **Arc:** moved from implementation+QA on `dont` into product-shaping work: evaluated real adoption value, turned the findings into proposals, then turned proposals into reviewed execution tickets.
- **Next:** pick the new proposal lane in dependency order — likely `dont-2nh` (structured repository evidence locators) or continue the pre-existing ready bug queue if shipping current behavior matters more than new capability work.

## Tasks created

## Decisions

### Sessions 16:51 (dont — 2 sessions)
- **Session 1 (workqueue 16:13 — bug triage):** fixed `dont-nud` (`--reason` help text now says "(required)" for trust/ignore); fixed `dont-736` (stale doctor remediation hint replaced with `ls` suggestion); fixed `dont-ttc` (prime `status_counts` now counts ignored+locked from both claims and terms); filed `dont-fwi` (doubted terms missing from `blocking[]`)
- **Session 2 (16:51 — OpenSpec implementation):** implemented all 3 P1 OpenSpec features shipped in dependency order:
  - `dont-2nh` — structured repo evidence locators: `dismiss` gains `--file`/`--lines`/`--anchor`/`--excerpt` flags; `StoreEvent.evidence` changed from `Vec<String>` to `Vec<Value>` so JSON locators survive Cozo's query parser; path traversal (`..`) refused; verify-evidence skips structured locators
  - `dont-7r9` — `dont trace <id>` blocker-path query: cycle-safe traversal with visited set; also fixed latent bug in `derived_assessments_for_claim` (was always returning "unresolved-term" instead of "stale-cascade") and fixed `conclude` to persist unresolved CURIEs in `depends_on`
  - `dont-tiv` — `dont ground <statement>` one-shot grounding: pre-validation atomicity, concluded→dismissed event sequence, empty statement/URI guards, `events[]` added to claim view
  - `dont-fwi` — prime `blocking[]` now includes doubted terms with `id/curie/status` shape
- Each feature got Ro5u review with findings applied before commit; all tests green throughout
- **Arc:** bug triage wave → all 3 OpenSpec P1 proposals fully implemented and shipped; `add-evidence-locators`, `add-ground-command`, `add-trace-query` proposals are now live code
- **Next:** `dont-dxu` (expose `ground` in help/completions/schema surfaces) or `dont-8bu` (drift-aware evidence projection); UX issues `dont-0um`/`dont-psu`/`dont-sxh` remain in backlog

### Sessions 16:37 (pretender — 2 sessions)
- **Session 1 (setup):** bootstrapped `pretender` repo with wai (design phase), beads (23→25 issues), openspec (project.md + 2 baseline specs); ran Rule-of-5-Universal review on all docs and applied 22 findings; ran issue-review across all 23 tickets, wired 24-edge dependency graph, created `pretender-9kx` (tracer bullet), `pretender-3b5` (report command), `pretender-vuc` (explain, P4)
- **Session 2 (implement):** advanced wai phase to `implement`; built the Cargo workspace — `model.rs` (universal code model: Module/CodeUnit/Block/Branch/Node), `metrics.rs` (cyclomatic complexity as pure function), `python.rs` (tree-sitter adapter for Python), CLI binary, fixture `python_simple.py`; all 6 callables detected with correct scores (`complex_func: 6`, `method_with_loop: 3`, `with_branch: 2`, three at `1`); closed `pretender-9kx`
- **Architecture decision:** universal model (Module→CodeUnit→Block→Branch) sits between tree-sitter and metrics — each language adapter produces the model; metrics are language-agnostic
- **Arc:** zero code → full tracer bullet in one session; architecture is validated end-to-end
- **Next:** P1 wave now unblocked — `pretender-b5o`, `pretender-jy9`; or `pretender-8ai` (5 additional languages)

### Session 17:05 (Testimonial.jl)
- Set up `Testimonial.jl` repo from scratch with the full `wai`/`bd`/`openspec` toolchain following the "wai way"
- Initialized `wai` (PARA structure, `.wai/AGENTS.md`), `bd` (beads issue tracker, Dolt backend, Claude Code hooks), and `openspec` (specs directory, project.md)
- Created `justfile` with install/test/record/run/info/specs/doctor/check recipes
- Authored `openspec/project.md` with full Testimonial.jl domain model, capability map, tech stack, conventions, and constraints
- Scaffolded `implement-coverage-layer` OpenSpec Phase 1 MVP proposal: `proposal.md`, `design.md`, and 3 spec files (`coverage-index`, `recording`, `smart-selection`) plus `tasks.md` with 30+ ordered work items
- Ran Rule-of-5-Universal review on the entire setup; found 22 issues (2 CRITICAL, 9 HIGH, 7 MEDIUM, 4 LOW)
- Applied all fixes: cache key format canonicalized, `ItemCoverage` struct specced, `driver.jl` contract specced, `TestimonialRunner` workspace requirement added, Phase 3 bleed removed from Phase 1 specs, atomic per-item write requirement added, corrupted index error handling specced, empty diff + non-Julia file + timeout cleanup edge cases added, sync comments added to `CLAUDE.md`/`AGENTS.md`
- **Next:** approve `implement-coverage-layer` proposal (`openspec approve implement-coverage-layer`), then create beads issues from `tasks.md`, and start Phase 1 implementation with `bd ready`

### Session 19:55 (dont)
- Completed QA pass of the `dont` CLI, identifying critical ergonomics gaps and lifecycle incompleteness.
- Formulated and committed OpenSpec change proposal `improve-cli-ergonomics` covering verb renames (trust→doubt, dismiss→verify), default human-readable output, and CURIE/short-ID resolution.
- Added requirements for hypothesis and atom management to reach 'Locked' status.
- Verified and updated 4 existing `bd` issues (`dont-0um`, `dont-psu`, `dont-jr8`, `dont-fwi`) and created `dont-1f4` for missing hypothesis/atom commands.
- Committed tracked proposals for `add-evidence-locators`, `add-ground-command`, and `add-trace-query` to maintain source traceability.
- **Next:** Implement the human-readable output mode and CURIE resolution logic.

### Sessions 20:37 (dont — 5 sessions)
- **Ergonomics/inspection sprint (workqueue):** shipped `--human` output mode (`dont-0um`), CURIE lookup in `show` (`dont-jr8`), shell completions + `ground` help/stdin rejection (`dont-dxu`), and `hypothesis add`/`hypothesis assess` (`dont-1f4`); Ro5 review fixes made `Locked` reachable end-to-end and cleaned several projection/performance issues.
- **Current session:** implemented `dont-8bu` drift-aware repository evidence projection in `src/main.rs` with new coverage in `tests/evidence_drift.rs`; repo locators now project excerpt/fingerprint/audit status in `show`, `why` exposes a matching WhyView projection, and `verify-evidence` reports drift/unresolved line spans without mutating persisted status.
- Closed `dont-8bu`; committed and pushed `a45b462 feat: add drift-aware evidence projection (dont-8bu)`; pushed beads/Dolt state.
- Validation: `just test` passed; `openspec validate --all --strict` passed. `just lint` still fails on pre-existing issues (duplicate `#[test]` in `tests/trace.rs`, plus clippy collapsible-if warnings outside this slice).
- Workspace note: OpenSpec spec/archive files for `add-evidence-locators` remain modified/untracked from earlier sessions and were intentionally left separate from the `dont-8bu` code commit.
- **Arc:** CLI ergonomics + repo-grounding sprint moved `dont` from JSON-only core verbs toward inspectable, auditable repository-grounded memory: human output, CURIE access, completions, ground/hypothesis workflow, and drift-aware evidence checks are now all shipped.
- **Next:** pick `dont-unm` (teach repository-grounding workflow in help/tutorials) or `dont-cw6` (rollout sequencing review); separately clean up/commit remaining OpenSpec files if they are intended source-of-truth updates.
