# 2026-05-11

## Focus
- Ghost overlay sensor pipeline correctness

## Log

### Session 17:31 (tRAGar)
- Closed `tRAG-vae` — resolved rerank ordering ambiguity: explicit guarantee "rerank-all-then-yield" for v0.1, forward-compat note for v0.3 streaming opt-in via `Reranker.supportsStreaming` + `QueryOptions.streamingRerank`
- Updated `openspec/specs/public-api/spec.md` with the explicit ordering guarantee
- Archived `add-tracer-bullet-vertical-slice` OpenSpec (implementation was already done, tasks hadn't been checked off)
- Implemented `add-vector-access-export` OpenSpec fully (9/9 tasks):
  - `getVector(id)` → `Float32Array` copy, throws `NotFound` for unknown ids
  - `getAllVectors()` → snapshot array of `{ id, v: Float32Array }`
  - `export('json')` → `{ meta, chunks, vectors_b64 }` Blob
  - `export('binary')` → STORE-method zip (pure JS, no deps)
  - `DequantizationRequested` warn-once pattern
  - 19 new tests (98 total, all passing)
- Implemented `add-file-store` OpenSpec (tasks 1.1–1.4, 1.7–1.8 done):
  - `stores.file(dirPath)` factory in `js/tragar.ts`
  - `NodeFsFileBackend` with dynamic `node:` imports for browser-safe bundling
  - 13 tests; tasks 1.5–1.6 blocked on `add-vocabulary-chunker`
- Rule-of-5 review pass on `add-vector-access-export`:
  - Fixed `#fireDequeueWarn` typo → `#fireDequantWarn`
  - Improved `export()` JSDoc to describe binary format schema
  - Clarified misleading test name
- **Next:** pick next OpenSpec track (`add-vocabulary-chunker` unblocks file-store 1.5–1.6; or start another 0% track)

## Tasks created

## Decisions
