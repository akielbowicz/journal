# 2026-05-18

## Focus
- what the day is about

## Log

### Session 13:23 (charly)
- Audited CI across all repos in the charly project directory (`dont`, `tRAGar`, `nayra`)
- Fixed **tRAGar** (TypeScript): added `allowImportingTsExtensions: true` to tsconfig and installed `@types/bun`
- Fixed **nayra** (Playwright): skipped resize/RAF test on Firefox headless (same as webkit), which doesn't fire rAF from resize events
- Fixed **dont** (Rust) — cascading series of issues:
  - Corrected expected exit code in test (1 → 3 for `ConfigMissing`)
  - Committed `.dont/` project structure (was fully gitignored) to satisfy `dont prime` prek hook in CI
  - Fixed test broken by committing `.dont/`: test was discovering the repo's own `.dont/` via dir traversal — pointed it to a nonexistent path via `DONT_DIR`
  - Committed `.vale/styles/Local/` and `.wai/` subdirs (7 dirs) that existed locally but were empty/untracked
- All three repos are green after the fixes
- **Next:** nothing outstanding

## Tasks created

## Decisions
