# 2026-05-27

## Focus
- notes tooling and Emacs/zk workflow

## Log

### Session 17:32 (la-vida-privada-de-los-pensamientos)
- Fixed `zk` editor integration for Nushell/Emacs by configuring `.zk/config.toml` to use `emacsclient --socket-name server -c -a emacs`.
- Investigated Emacs daemon health, stopped/restarted `homebrew.emacs.service`, and verified the daemon socket at `/run/user/1000/emacs/server`.
- Updated `justfile` so `just poo` without a title uses `fzf` over recent POO notes and added `just recent [LIMIT]` for recent-note selection.
- Fixed titled POO note creation to use the `poo.org` template explicitly via `zk new ... --template poo.org` instead of falling back to the default note template.
- Committed and pushed repo changes: `Configure zk to use Emacs` and `Improve POO note creation and Emacs integration`.
- **Next:** Verify the full `jg poo` flow end-to-end in normal use; `.zk/notebook.db` is still a local uncommitted change.

### Sessions 18:10 (paranoid — 2 sessions)
- **2026-05-15**: Reviewed and tightened `add-nn-adapt-lab` OpenSpec proposal — pinned LiteRT runtime, committed to Room for persistence, updated tasks/proposal, validated with `openspec validate --strict`, pushed `5a116a2`.
- **2026-05-27**: Debugged VLC install hanging on Bluefin (immutable Fedora Atomic); Flatpak system OSTree store deadlocked at apply phase for both GL drivers (142 MB) and VLC itself.
- Identified root cause: system Flatpak store contention on Bluefin's read-only base image management.
- Fix: added Flathub as a user remote (`flatpak remote-add --user`) and installed with `flatpak install --user --no-related flathub org.videolan.VLC` — writes to `~/.local/share/flatpak/` instead of the system store.
- **Arc:** two separate topics (OpenSpec work + Bluefin Flatpak debugging) in the same paranoid project session window.
- **Next:** evaluate `add-nn-training-lab` proposal for feasibility and cross-check shared-domain types (`NnExample`, `NnPrediction`) against adapt-lab contract.

### Session 18:12 (test — Julia GPU / ROCm on Bluefin)
- Verified AMD Radeon 780M (gfx1103 / RDNA3) is GPU-capable: ROCm kernel driver loaded, `AMDGPU.jl` detects device.
- Root cause of AMDGPU.jl failure: system ROCm libs are versioned (`.so.7`) but `find_roc_path()` needs unversioned `.so` symlinks.
- Fixed without root: created `~/.local/rocm/lib64/` with symlinks; setting `ROCM_PATH=$HOME/.local/rocm` makes Julia find the GPU.
- Added `ROCM_PATH` to nushell `env.nu`, tracked with chezmoi, committed and pushed to dotfiles repo.
- Lux.jl on AMDGPU: basic ops work, but rocBLAS (dense layers) and MIOpen (conv/batchnorm) are missing.
- On Bluefin (immutable OS), options for ROCm libraries: `rpm-ostree install` (needs reboot) or distrobox.
- Evaluated distrobox images: `rocm/rocm-terminal:latest` (~1.1 GB compressed, ~3–4 GB on disk) vs `ubuntu:22.04` + manual ROCm.
- Julia in distrobox: `/home/linuxbrew` not mounted → host julia unavailable; shared `~/.julia/` causes glibc cache conflicts.
- **Decision:** isolate Julia inside distrobox with separate depot and juliaup install: `JULIA_DEPOT_PATH=~/.julia-rocm`, `juliaup` to `~/.juliaup-rocm`.
- **Next:** set up the isolated distrobox environment and verify AMDGPU.jl + Lux.jl work end-to-end inside it.

## Tasks created

## Decisions
- Use explicit `emacsclient --socket-name server` for `zk` editor commands instead of relying on implicit socket discovery.
- Use `--template poo.org` for titled POO note creation to guarantee the POO structure is rendered.
