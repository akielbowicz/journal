# 2026-05-05

## Log

### Sessions 09:40 (paranoid — 2 sessions)
- Closed OpenSpec change `add-usage-audit-history-drilldown` (30/30 tasks ✓): extracted `startOfLocalDay()` and `DailyUsageSummary.toAppRows()`, added DST/past-day aggregator tests, `just test` green, strict validation passing.
- Cut release **v0.7.0**: archived OpenSpec change, added CHANGELOG entry (UsageAudit History/Day Detail/scoped Share+CSV), tagged and pushed `main` + `v0.7.0` to `cv:charly-vibes/paranoid.git`. CI release workflow handles signed APK build.
- **Next:** verify GitHub Actions release run + APKs uploaded; manual on-device verification of UsageAudit usage-access handoff.

### Sessions 11:14 (dont — 8 sessions)
- Reviewed OpenSpec readiness; deferred label-validator work (`add-dont-define-label-validators`); applied stale-model fixes across status lifecycle, payloads, rule engine, lifecycle verbs, derived queries, error-code specs; committed `f56a14b spec: clarify stale as derived assessment`.
- Implemented all 10 tracer-bullet phases from scratch in a single session:
  - **Phase 1** — Rust crate scaffold, clap subcommands, CLI tests (`5a05824`)
  - **Phase 2** — CozoDB SQLite datom store with write locking, schema mismatch rejection, reopen persistence (`da605ad`)
  - **Phase 3** — `Envelope<T>`, `ErrorResult`, `Warning`, `HintEntry`, `Meta`; Invariant 3.2.5 enforced (non-empty remediation)
  - **Phase 4** — `Status` enum (Unverified/Verified/Doubted), `trust()`/`dismiss()` with typed `TransitionError`
  - **Phase 5** — `dont init`: `.dont/` scaffold, `config.toml`, `AGENTS.md`, parent-dir discovery, `DONT_DIR` override, `already-initialised`/`config-missing` errors
  - **Phase 6** — `dont conclude`: ULID-prefixed IDs, ClaimView envelope, datom persistence
  - **Phase 7** — `dont trust`: reason-required, reason-not-hedge (case-insensitive substring), invalid-transition, claim-not-found
  - **Phase 8** — `dont dismiss`: no-evidence refusal, evidence-only append for already-verified claims; fixed Cozo double-encoding by storing evidence as `Value::Array`
  - **Phase 9** — `dont show`/`dont list`: `updated_at`, ULID tiebreaker sort, batch query (N+1 → 2 queries)
  - **Phase 10** — 7 integration tests (multi-step workflows, envelope conformance, cross-process persistence, 50ms perf on 100 claims)
- Closed all 10 phase beads and the tracer-bullet epic (`dont-9iq`). 75 TDD tests, all green.
- **Arc:** went from bare spec to a fully working 6-command `dont` CLI binary in one session, proving Rust+CozoDB datom architecture end-to-end.
- **Next:** spec work (`dont-71y`, `dont-656`) or next tracer-bullet follow-on; run `bd ready`.

### Sessions 12:34 (superficies — 13 sessions)
- Built and shipped the Superficies tracer-bullet app: Vite/TypeScript single-file catalog with deterministic hash state, preset/live controls, hero layouts, specimen strip, token diagnostics, export workspace, GitHub Pages deployment, and responsive desktop/mobile editing chrome.
- Completed the original Beads epic and follow-up polish: long-hash base64 fallback, font-pair constraints, safe escaping/validation, dark-mode color cascade, scroll fix, Both mode, theme-aware panes, TUI/CLI archetypes, terminal hero layout, export topbar CTA, and About onboarding.
- This session reviewed the latest CSS fixes for similar regressions, fixed export-output theming, mobile Both-mode stacking, magazine action overflow, and retitled the app to “Visual system workbench”.
- Added export-modal polish: changing full-brief format preserves the open modal, and one-click downloads now save the full brief (`.xml`/`.md`) and token JSON (`.json`) with deterministic filenames/MIME types.
- Validation stayed green (`npm run build`; 44 tests after download additions), and changes were committed/pushed to `origin/main` as `116112d feat: add export downloads and polish preview layout`.
- **Decisions:** keep export downloads client-only via Blob/object URLs; keep full-brief extension tied to XML/Markdown format; keep token JSON as a separate direct-download action rather than overloading the active export tab.
- **Arc:** moved from research/spec setup through a deployed tracer bullet and complete epic, then into design-polish/productization: better archetypes, layout/theme correctness, and practical handoff downloads.
- **Next:** run browser smoke checks for the export download buttons and recent layout fixes; if continuing feature work, consider the previously noted color palette widget and start by running `wai search "color"` / `wai search "palette"`.


### Session 15:50 (paranoid)
- Verified the GitHub Actions `Release APK` run for `v0.7.0` completed successfully (3m 43s).
- GitHub Release [v0.7.0](https://github.com/charly-vibes/paranoid/releases/tag/v0.7.0) published with all three signed APKs: `paranoid-v0.7.0-arm64-v8a.apk`, `paranoid-v0.7.0-armeabi-v7a.apk`, `paranoid-v0.7.0-x86_64.apk`. Auto-generated notes link `v0.6.0...v0.7.0`.
- **Next:** on-device manual verification of UsageAudit usage-access Settings handoff and overnight battery snapshot behavior (carry-over from 2026-04-24 inbox).

### Sessions 16:01 (dont — 3 sessions)
- Resumed after tracer-bullet and spec-archive sessions; issue board was empty (0 open, 0 ready).
- Created 45 feature tickets across 5 spec epics: `dont-edn` (init/lifecycle, 8 tickets), `dont-td5` (project layout/config, 7), `dont-yve` (rule engine/CLI, 8), `dont-sap` (harness/help/spawn, 13), `dont-c4a` (import adapters, 9). All P2/feature, gated on their spec epic.
- Ran `/issue-review` on all 45 tickets — found 2 CRITICAL, 8 HIGH, 8 MEDIUM, 6 LOW issues across 5 passes.
- Fixed all issues:
  - **21 dependencies added**: 9 rule/query tickets now gate on `dont-8cs` (Cozo engine); `dont-i1v` gates on `dont-m0v` (define verb); 8 import adapters gate on `dont-3ul` (shared infrastructure); 3 spawn verbs gate on `dont-2tl` (LLM provider config).
  - **3 scope-split tickets**: `dont-e7b` (Diataxis content, HITL, split from `dont-zoc`); `dont-rws` (`dont spawns` inspection, split from `dont-lei`); `dont-d3r` (`dont examples`, split from `dont-867`).
  - **Description quality**: SK11 jargon replaced, version-scoped constraint clarified, AFK/HITL boundaries labelled on spawn verbs, race-resolution rule extracted into `dont-a84`, MCP smoke-test command added to `dont-5hg`, source-specific normalisation rules enumerated for 7 adapter tickets, `Done when:` blocks with `cargo test` + observable CLI output added to all 48 tickets.
- **Arc:** went from 0 tickets to a fully groomed 48-ticket backlog with correct dependency ordering — no false-ready states.
- **Next:** `bd ready` will show 0 unblocked tickets (all gate on spec epics); next step is to open the first spec epic (`dont-edn` or `dont-td5`) and start implementing.

### Session 17:02 (gh)
- Configured the `lz` Git/GitHub setup for the `lazlowicz` account under `/var/home/sasha/para/areas/dev/gh/lz/`.
- Added per-directory Git profile `~/.config/git/profiles/lz` with `user.name = lazlowicz` and `user.email = lazlowicz@proton.me`.
- Added URL rewrite rules in `~/.config/git/private` so `https://github.com/lazlowicz/...` and `git@github.com:lazlowicz/...` resolve to the `git@lz:lazlowicz/...` SSH alias.
- Generated and configured `~/.ssh/id_lz` plus `~/.ssh/profiles/lz`; corrected the SSH profile to use GitHub's required `User git`.
- Verified `ssh -T lz` authenticates successfully as `lazlowicz`.
- **Next:** use `/var/home/sasha/para/areas/dev/gh/lz/` for `lazlowicz` repositories; clone with standard GitHub URLs and let the rewrite choose the `lz` SSH key.
