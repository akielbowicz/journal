# 2026-05-07

## Focus
- what the day is about

## Log

### Session 15:19 (wai)
- Made built-in pipeline templates discoverable in `wai pipeline --help -v` and `wai pipeline init --help -v`.
- Refreshed stale pipeline help from legacy `create`/`run`/`advance` and `.yml` wording to current `init`/`start`/`next` and `.toml` definitions.
- Centralized built-in pipeline template names and added tests covering help output and template registry consistency.
- Ran Rule of 5 review, fixed all findings, bumped release to `2026.5.0`, updated `CHANGELOG.md`, committed, tagged `v2026.5.0`, and pushed `main` + tag.
- **Next:** Watch CI/release automation for `v2026.5.0` if applicable.

## Tasks created

## Decisions
