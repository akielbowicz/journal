# 2026-05-07

## Focus
- what the day is about

## Log

### Session 15:19 (wai)
- Made built-in pipeline templates discoverable in `wai pipeline --help -v` and `wai pipeline init --help -v`.
- Refreshed stale pipeline help from legacy `create`/`run`/`advance` and `.yml` wording to current `init`/`start`/`next` and `.toml` definitions.
- Centralized built-in pipeline template names and added tests covering help output and template registry consistency.
- Ran Rule of 5 review, fixed all findings, bumped release to `2026.5.0`, updated `CHANGELOG.md`, committed, tagged `v2026.5.0`, and pushed `main` + tag.
- **Next:** Watch CI/release automation for `v2026.5.0` if applicable.

### Session 15:59 (general)
- Investigated context/token costs of `/next`, `/renew`, `/close` across `~/.pi/agent/sessions` and `~/.pi/agent/traces`.
- Root causes: monolithic `workqueue.md` (full-file scan per slug), full daily log file reads (up to 3 days), inbox loading upfront, skill body injected inline.
- Designed 6 changes: per-slug `queue/$SLUG.md` files, lazy grep-by-section log extraction, 1–2 day window (kept at 3), skip queue scan when empty, safe Write/Edit writes over heredocs, compact multi-session output preserving **Next:** bullets.
- Ran Rule of 5 review — found 1 CRITICAL (migration split-brain during deploy), 5 HIGH, 8 MEDIUM, 3 LOW issues.
- Fixed all findings: migration order defined (legacy → new, by timestamp ascending), grep uses `-nF` with escaped parens, inbox behavior preserved (no regression), slug sanitized via `tr`, git add covers both paths during transition.
- Updated SKILL.md for all three skills (source in journal repo + deployed to `~/.agents/skills/`).
- **Next:** monitor first real `/renew` + `/close` runs after deploy to confirm token costs drop.

### Sessions 17:05 (dont — 2 sessions)
- **Session 1 (15:57):** Scaffolded the `improve-cli-ergonomics` milestone — created 7 beads tickets (`dont-ehv`, `dont-9xa`, `dont-08l`, `dont-fb1`, `dont-khn`, `dont-72t`, `dont-3so`), ran two rounds of issue-review (split `dont-08l`, added "Done when:" criteria, added `dont-72t` → `dont-08l` dep, formalized `dont-3so` validation criterion).
- **Session 2 (17:05):** Implemented three features under TDD + Ro5 review:
  - `dont-ehv` (spec sync): Rule of 5 review found 3 CRITICAL + 8 HIGH issues (all `doubt`/`verify` verb mismatches vs. shipped `trust`/`flag`/`undoubt`); rewrote three `improve-cli-ergonomics` delta specs; committed `a8d2590`.
  - `dont-khn` (universal flags): added `--author/-a`, `--direct`, `--plain`, `--version [--json]` globally; author stamped into every envelope `Meta`; 7 tests; committed `cae0d7a`.
  - `dont-fb1` (CURIE/short-ULID resolution): added `Store::resolve_entity()` with `AmbiguousPrefix`, case-insensitive prefix scan, empty-input guard; simplified `show`/`why` dispatch; Ro5 fixed 2 HIGH issues post-implementation; 9 tests; committed `f326371`.
- **Arc:** `improve-cli-ergonomics` moved from 1/16 (6%) to 4/16 (25%); `dont-ehv`, `dont-khn`, `dont-fb1` closed.
- **Next:** Continue with `dont-9xa` (default human-readable output) or `dont-72t` (hypothesis/atom subcommands).

### Sessions 17:05 (atril — 8 sessions)
- **Sessions 1–7 (12:49–15:44):** Full implementation of `add-openspec-workspace-view`: reconciled navigator/cross-links overlap; added `projectDocuments` to `OpenSpecIndex`; rendered workspace overview with first-class change cards (`parseTaskSummary`, missing-doc indicators, resilient incomplete-dir handling); wired all navigation cross-links (specs↔changes↔archive); extended `OpenSpecIndex` with `archivedChangeFiles`/`archivedChangeAffects`; 385 tests passing; all committed and pushed.
- **Session 8 (17:05):** Drove Playwright walkthrough to assess UI quality; identified 3 issues; created tickets; fixed all three under TDD + Ro5 review:
  - `atril-yd2` (P1): removed duplicate `specs-toc` nav from workspace Specs section; Ro5 cleanup — dead scroll CSS, orphaned `id="spec-…"` anchors, stale "legacy" JSDoc, extra blank line.
  - `atril-cq2` (P2): grouped file search results by directory with labelled section headers (Map-based); removed dead `tree-search-path` CSS; tests updated.
  - `atril-25s` (P2): mobile full-screen drill-down — `has-selection` CSS class on `.beads-layout` driven by explicit vs auto-resolved selection; `← Back` button clears URL + re-renders; Ro5 found MEDIUM (blank header for root-level files when `dir === ""`), plus root path slicing bug (`lastIndexOf("/") === -1` → wrong dir) and `hasExplicitSelection` false positive (filtered-out ID still truthy); all fixed with 3 new tests.
- **Arc:** `add-openspec-workspace-view` fully implemented; UI polished across desktop + mobile; 389 tests passing; 5 commits pushed.
- **Next:** `atril-rtm` — manual smoke test against charly-vibes/atril; `atril-amw` — validation walkthrough still pending HITL.

## Tasks created

## Decisions
