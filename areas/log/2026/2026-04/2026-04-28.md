# 2026-04-28

## Focus
- paranoid: UsageAudit mini-app UI, export, hub integration

## Log

### Session 09:15 (paranoid)
- Implemented PARANOID-c30: Today & Last Night screens (5A/5B/5D)
  - Pure presentation layer (`TodayScreenPresenter`, `LastNightScreenPresenter`) with sealed state types
  - Today: empty state + populated summary with top apps by duration
  - Last Night: battery delta, observed/suspected heading, warning flags
  - Attribution disclaimer, dark theme (#121212), OLED-friendly layout
  - 12 unit tests for all presentation states and wording rules
  - Ro5 review caught scope leak (CORR-001) — fixed with `lifecycleScope`
- Implemented PARANOID-a45: Export and sharing (4A/4B/4C)
  - `TodaySummaryFormatter`, `LastNightSummaryFormatter` for plain-text share
  - `CsvExporter` with v1 schema (10 columns) and proper CSV escaping
  - `UsageAuditShare` for text and CSV (FileProvider) sharing
  - Share Summary / Export CSV buttons wired into both sections
  - 10 unit tests; Ro5 caught null-percent formatting bug — fixed
- Implemented PARANOID-7fq: History screen (5C)
  - `HistoryScreenPresenter` with date/delta/app-count/warning entries
  - Recent Nights section (last 6 nights) with tap-to-share
  - Extended `UsageAuditDataProvider` with `UsageAuditData` wrapper
  - 3 unit tests
- Implemented PARANOID-dlt: Hub integration (ticket 6)
  - Added UsageAudit to `AppRegistry` — launchable from main hub
- Closed previously-implemented beads: bmm, 7mh, 4tb
- Ran validation (ticket 7): unit tests green, wording audit clean
- **Decisions:** presentation logic kept pure Kotlin (no Android deps) for unit testability; reused existing `FileProvider`/cache exports pattern; `lifecycleScope` for all async work
- **Next:** remaining items are manual device verification (7.2–7.7, 7.11–7.12) and two P3 NetDiag instrumentation test tickets

### Session 11:04 (incitaciones)
- Added the new `grill-me` skill to `incitaciones`, adapting the upstream Matt Pocock skill into repo format with `content/prompt-task-grill-me.md` and `content/distilled/grill-me.md`
- Registered `grill-me` in `content/manifest.json`, added it to the `planning` and `reviews` bundles, and validated the manifest with `just sync-manifest`
- Used the requested YouTube video as the source reference and kept the upstream GitHub skill link in the prompt references for provenance
- Committed and pushed the skill in `d545fad` (`feat(prompts): add grill-me planning interview skill`)
- Added and pushed a changelog entry in `631eabc` (`docs(changelog): record grill-me skill addition`)
- **Next:** decide whether the local untracked `content/compiled/` output in `incitaciones` should be cleaned, ignored, or intentionally tracked

### Session 17:32 (wai)
- Ran `wai way` — identified missing ubiquitous language configuration
- Scaffolded `.wai/resources/ubiquitous-language/` with `README.md` index and 5 bounded-context glossaries: `projects`, `artifacts`, `sessions`, `pipelines`, `plugins`
- Each glossary defines terms with definition, anti-terms, and related terms
- `wai way` now shows green check for ubiquitous language
- Committed and pushed in two commits: `a944453` (glossaries) + `c2bfba9` (managed block / config bumps)

### Sessions 17:31 (atril — 3 sessions)
- Session 1 (09:48): accessibility/navigation pass — semantic `<button>` for overview/tree/breadcrumb, clickable overview source badges, inline SVG favicon; OpenSpec cross-linking (auto-linking in `document-renderer.ts`, clearer active/archived indexing, canonical spec links for breadcrumbs)
- Session 2 (15:26): UX improvements — default-open OpenSpec sections, first-issue auto-selection in beads list/detail, centered animated loading UI, issue-search empty-state with inline clear-search button (`atril-avn`, `atril-wbl`, `atril-ch4`, `atril-7lh`)
- Session 3 (17:31): batch of 10 tickets via TDD → Ro5 review → commit loop:
  - `atril-67n`: branch toggle chevron + visible border/padding at rest
  - `atril-02x`: filter empty state names active criteria; inline Clear filters; hides detail panel
  - `atril-4w4`: copy-link button in file header + issue detail header (clipboard API, 1.5s "Copied!" state)
  - `atril-fmf`: WCAG keyboard accessibility — issue rows converted from `<li>` to `<button type="button">`; focus-visible ring CSS
  - `atril-5kz`: table of contents for spec files (`buildToc`, ≥3 headings threshold, h3 indent)
  - `atril-b55`: error screen contextual hints + pre-filled "Try again" URL + `role="alert"`
  - `atril-ogx`: theme toggle ☀/☾/◑ icons + improved contrast
  - `atril-05l`: freshness date now shows year
  - `atril-k87`: mobile filter bar collapses behind "Filter ▾" toggle at ≤480px
  - `atril-yd4`: history view shows author + date on every row, title truncated at 60 chars
  - `atril-9q3`, `atril-cpp`: already shipped — file search dot-dir penalty and spec tree dedup
  - `atril-9m2`, `atril-o6f`: already implemented — tasks/tests updated and tickets closed
- **Arc:** day-long sprint through `atril` accessibility, UX polish, and OpenSpec integration — 275 tests passing, all committed and pushed
- **Next:** `bd ready` for next unblocked ticket; unrelated worktree files (`.wai/resources/scripts/explore-video.ts`, `chrome`) remain untouched

### Session 17:33 (XAct.jl)
- `grill-me` architecture interview on Tequitl — the new Julia tensor CAS to replace XAct.jl's string engine
- Decisions locked in:
  - Migration: parallel track (Option C) — Tequitl in separate `chacana/tequitl` repo; old string engine not extended
  - Session type: `Session{S}` with phantom `S` for dispatch + runtime `AbstractContext` field for live tensor/manifold registry
  - Parser: Julia-native via `ccall` into tree-sitter C library; two-phase design (context-free unresolved AST → session-aware resolution)
  - Error model: soft failure with `Result` type; roll-your-own `Ok{T}` / `Err{E}` with `Vector{ResolutionError}` (no `Either.jl` dep)
  - Integration test oracle: numerical evaluation (substitute random values, compare floats)
  - Symbolics.jl integration: `TensorTerm <: Symbolics.Symbolic` — Tequitl lives inside the term algebra (already prototyped and working)
  - Covariant derivative: `∇[g]_μ` carries metric symbolically; generic rewrite rule parameterized by rank/index slots at rewrite time
- Note saved to `borradores/exploraciones/tequitl-xact-architecture-grilling.org`
- **Next:** prototype generic covariant derivative rewrite rule in Symbolics.jl; validate that index metadata survives `simplify`/`substitute`

## Tasks created

## Decisions
- UsageAudit presentation layer is pure Kotlin sealed interfaces — testable without Robolectric
- `UsageAuditDataProvider` passes `CoroutineScope` from caller (Activity) to avoid leaked scopes
- History generates overnight audits on-the-fly from snapshots (no separate reports table)
- "Suspected contributors" heading only when batteryDeltaPercent < 0; otherwise "Observed activity"

### Session 19:52 (atril)
- Added `language` source to the overview knowledge-source badge row (between Memory and Docs)
- Detection: any blob under `.wai/resources/ubiquitous-language/contexts/*.md` sets `language: true`
- Active badge navigates to `view=wai&section=language`; inactive repos get a greyed-out span with tooltip
- Changes across three files: `repo-overview.ts` (type + detection + suggestion), `navigation-renderers.ts` (badge label + inactive title), `main.ts` (`kind === "language"` routing branch)
- Extended `explore-video.ts` with 7 language explorer scenarios: WAI entry click, bounded-context list, glossary drill-down, deep-links, term anchor, invalid-context fallback
- Screenshots regenerated; all 340 tests passing
- Committed in two atomic commits (`feat(overview)` + `chore(explore-video)`) and pushed
- **Next:** `bd ready` for next unblocked ticket in the ubiquitous-language-explorer epic

### Session 20:44 (atril)
- Implemented ubiquitous language explorer feature end-to-end via TDD ticket-cycle pipeline:
  - `hasUbiquitousLanguage` predicate in `file-tree.ts` (detects `.wai/resources/ubiquitous-language/`)
  - `renderLanguageEntry` in `wai-overview.ts` — Language card button in WAI view
  - `language-explorer.ts` — `listBoundedContexts`, `extractGlossaryTerms`, `renderGlossary`, `renderLanguageOverview`
  - Router extended with `section`, `context`, `term` params for deep-link URL support
  - `main.ts` wired for Language explorer routing (`showLanguageView`, back navigation, anchor scroll)
- Ro5 review caught two bugs: `showWaiView` not awaited in `routeTo`; invalid-context fallback showed empty list instead of full overview — both fixed
- Styled the Language button as a card (`abe0e1f`) — was a default browser button, easy to miss
- Diagnosed `charly-vibes/wai` parsing failure: its README uses bullet-list format and context files use `## Heading` + `**Definition:**` style (not pipe tables)
  - Extended both parsers to handle bullet-list README and heading-definition glossary alongside existing table formats
  - `extractHeadingDefinitionTerms` added as fallback in `extractGlossaryTerms`
  - `parseBulletContextList` added as fallback in `listBoundedContexts`
- 350 tests passing (up from 335); 13 commits pushed
- **Next:** `atril-rtm` — HITL browser smoke test to verify language explorer navigation end-to-end

### Session 22:57 (atril)
- Added `view=specs` spec bundle view — all `openspec/specs/*/spec.md` files fetched in parallel and rendered as a single shareable document
  - URL: `?owner=X&repo=Y&branch=main&view=specs`
  - Global TOC at top; each spec in its own `<section id="spec-<name>">` with preserved per-spec internal TOC and OpenSpec formatting
  - Copy-link button (🔗) in specs nav bar for sharing
- Clicking the "Specs" source badge now goes to `view=specs` instead of the file tree (when canonical specs exist)
- Router: added `"specs"` `ViewType` and `RouteTarget`; `buildRoute`/`parseRoute` extended
- `repo-overview.ts`: suggestion uses `kind: "openspec"` when canonical specs exist; falls back to `kind: "tree"` for change-only repos (no regression)
- `navigation-renderers.ts`: removed openspec-as-tree special-case in `renderSourceBadges`
- `main.ts`: `showSpecsView()`, screen refs, busyRegion, routeTo handler, back button, copy-link handler; removed dead `kind === "tree"` branch from `navigateOverviewItem`
- CSS: specs-nav/breadcrumb/content added to shared nav/panel groups; full typography block mirroring `#file-content`; `.specs-toc`, `.spec-divider`, `.spec-section`, `#specs-actions` flex rule
- Ran Ro5 review — found 1 HIGH (kind regression for change-only repos), 1 MEDIUM (dead code), 1 LOW (missing flex CSS) — all fixed before push
- Committed `eecaa3f` and pushed
- **Next:** `bd ready` for next unblocked ticket

### Session 22:39 (SundaeVolatility)
- Ran `wai way` and used the result to scaffold `.wai/resources/ubiquitous-language/` for the repo
- Added a root glossary plus bounded-context docs for core simulation/pricing, gameplay/progression, and player-facing diegetic UI
- Ran the `rule-of-5-universal` review on the new docs, then fixed all findings:
  - added explicit usage policy and mixed-audience rules
  - clarified `deal` / `deal slip` / `contract` / `position`
  - added counterparty vocabulary, Mint-Chip migration guidance, identifier policy, and related-spec links
- Normalized repo docs to the glossary:
  - `README.md` updated to current `openspec/` structure and diegetic-first mixed-audience wording
  - `llm.txt` updated to current spec locations and glossary policy
  - `CLAUDE.md` and `AGENTS.md` now point agents to the ubiquitous-language source of truth
- Committed and pushed two repo commits:
  - `7e4fb08` — `docs: align repository language with diegetic glossary`
  - `735ae0c` — `chore: refresh wai metadata and ignore local agent settings`
- Added `.claude/settings.local.json` to `.gitignore`
- **Decisions:** player-facing language standardizes on promise/tool/weather/flavor; mixed-audience docs use diegetic term first and technical term in parentheses on first mention; canonical doc/spec naming prefers Mint-Chip
- **Next:** decide how to handle untracked `src/` and `tests/` in `SundaeVolatility` before they drift further
