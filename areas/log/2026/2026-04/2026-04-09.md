# 2026-04-09

## Focus
- Set up BuJo/PARA work journal with AI agent skills

## Log

### Session 15:45 (journal repo — BuJo/PARA setup)
- Reviewed the work setup (`~/para/projects/work/dev/status/` and `dev/dotfiles/`) to understand the existing BuJo+PARA+skills pattern
- Audited current personal machine: found PARA dirs partially set up, beads for issue tracking, wai for project lifecycle, 50+ dev skills already deployed to `~/.config/agents/skills/` (also copied to `.agents/`, `.gemini/`, `.config/amp/`; claude symlinks to `.config/agents/`)
- Decided: journal repo is source of truth for BuJo lifecycle skills; sync to `~/.config/agents/skills/`; use `$JORNAL` env var for portability; no beads integration
- Restructured `~/para/areas/dev/gh/ak/journal/`: moved `jornal/` entries into `log/YYYY/YYYY-MM/`, `laboratorio/` into `resources/`, added full PARA dirs with `_index.md`
- Created 10 lifecycle skills: morning, capture, jlog, close, park, resume, migrate, standup, weekly, archive
- Created `bin/sync` with git commit provenance metadata in deployed copies
- Updated justfile and zk config for new paths
- Symlinked `~/para/areas/jornal` → repo
- Symlinked `CLAUDE.md` → `AGENTS.md`
- Set `$JORNAL` in fish (`~/.config/fish/config.fish`) and nushell (`~/.config/nushell/env.nu`)
- Deployed 10 skills, verified Claude picked them up (63 total)
- **Next:** fix other tools (`.agents/`, `.gemini/`, `.config/amp/`) to symlink to `.config/agents/skills/` via `incitaciones install`; start using the skills in daily workflow; populate projects/areas as work comes in

### Session 17:29 (journal repo — site, mise, polish)
- Restructured repo to pure PARA top-level: moved `skills/` → `resources/skills/`, `log/` → `areas/log/`; updated all path references in skills, AGENTS.md, bin/sync, zk config
- Added detailed README.md with full explanation of PARA, BuJo notation, skills, setup
- Set up GitHub Pages: started with Jekyll + minimal theme, then replaced with custom pandoc-based build (`bin/build`) for terminal-style monospace rendering
- Built `bin/generate-indexes` for auto directory listings, `bin/generate-search-index` + Lunr.js for client-side fuzzy search
- Added pandoc HTML template (`bin/template.html`): dark/light mode, breadcrumb navigation on every page, 80ch monospace layout
- Fixed org-mode rendering: `bin/convert-org` converts `.org` → `.md` via pandoc at build time; build uses staging copy to avoid mutating repo
- Fixed breadcrumbs: added `BASE_URL` support for GitHub Pages subpath (`/journal`)
- Replaced justfile with mise (`.mise.toml`): tools (pandoc, zk) + tasks (build, serve, deploy, jrn, lab, etc.)
- Fixed search: replaced Liquid template syntax with dynamic script loading for plain pandoc output
- Fixed homepage: prepends nav bar (inbox | projects | areas | resources | archive | search) + README content
- **Next:** fix symlinks for other agent tools via `incitaciones install`; style tweaks if needed

### Session 17:47 (wai repo — model config question)
- Answered question about current Claude Code model (Opus 4.6 with 1M context)
- Researched how to configure the same model on another PC: `~/.claude/settings.json` with `"model": "opus[1m]"`, env var `ANTHROPIC_MODEL`, or CLI flag `--model`
- Clarified difference between `opus[1m]` (auto-upgrades) and `claude-opus-4-6[1m]` (pinned version)

### Session 18:03 (incitaciones — symlink consolidation & install metadata)
- Fixed skills duplication: 4 separate copies of 50 incitaciones skills across `~/.agents/`, `~/.config/agents/`, `~/.gemini/`, `~/.config/amp/` → consolidated to single source at `~/.config/agents/skills/`
- Set up symlinks: `~/.agents/skills`, `~/.gemini/skills`, `~/.config/amp/skills` all now symlink to `~/.config/agents/skills/` (claude already did)
- Updated `install.sh`: added `skills_dir_is_symlinked()` check so install detects symlinks and skips redundant copies; `copy_skill_tree()` now detects same-dir copies; `do_uninstall()` respects symlinks
- Added version metadata to installed SKILL.md frontmatter: `installed-from: incitaciones`, `installed-version: "<sha> (<date>)"`, `installed-at: "<ISO timestamp>"`
- Investigated namespacing skills under `incitaciones/` subdirectory — not possible, Claude Code only discovers skills one level deep (`skills/<name>/SKILL.md`)
- Committed and pushed as `073aeb5`
- **Final state:** 63 skills (50 incitaciones + 13 custom), single source of truth, all 4 tools share via symlinks

### Session 18:29 (smithy — OpenSpec composability review and fixes)
- Reviewed all OpenSpec specs under `openspec/specs/*/spec.md` using the `composability-diagnostician` skill
- Identified composition friction in the spec contracts: mixed `Decision` vs `Outcome` carriers, retry-to-cascade signaling via `:cascade?`, unspecified best-candidate fold semantics, overloaded generic Proposal lifecycle, and middleware extension coupled to concrete layer names
- Updated OpenSpec docs to standardize stage results on `{:ok ...} | {:fault ...} | {:escalate ...}`, make retry exhaustion return explicit escalation results, formalize cascade candidate selection as a deterministic associative fold with law-testing requirements, and split Proposal handling into kind-specific lifecycle rules
- Synced related CLI wording so topology proposal review and creation match the new lifecycle model
- Logged the review and design rationale in smithy's `wai` artifacts, created a handoff, committed the smithy repo change as `afd75e7`, and pushed it to `origin/main`
- Noted one tooling gap: `bd sync` is not available in this repo's `bd` binary (`unknown command "sync"`)
- **Next:** if smithy implementation work starts, align runtime/code paths with the updated stage-result, cascade, and proposal contracts

## Tasks created
- `wai-54xo` epic: Docs onboarding — explain the why behind wai (P2)
- `wai-3vno` [A] Rewrite introduction.md as Why Wai? page
- `wai-rt31` [B] Add Why? sections to concept docs
- `wai-79mc` [C] Slim README to intro + install + quick start + links
- `wai-16dj` [D] Expand design principles with examples
- `wai-ogf9` [E] Annotate quick-start with why-this-matters

## Decisions
- Beads `dolt_mode: embedded` is the right default — server mode's zombie problem is a known upstream issue with no reliable fix
- Context pressure wording: option C ("stop and tell the user — responses degrade") over options A/B
- Journal skills are tool-agnostic (AGENTS.md, not CLAUDE.md-only); CLAUDE.md is a symlink
- Existing personal journal (org-mode/zk) coexists alongside the markdown BuJo/PARA layer
- No beads integration in journal skills — keep them separate
- `bin/sync` only deploys journal-owned skills, doesn't touch existing dev skills
- Top-level is pure PARA: skills live in `resources/skills/`, logs in `areas/log/`
- Dropped Jekyll in favor of custom pandoc build — simpler, no Ruby deps, full control over layout
- Replaced justfile with mise — single tool for deps + tasks
- `BASE_URL` env var controls site prefix (empty for local, `/journal` for GH Pages)
- `~/.config/agents/skills/` is the single canonical skills directory; all tools symlink to it
- Skills can't be nested under a namespace subdirectory — Claude Code only scans one level deep
- `install.sh` stamps version metadata (git sha, date, timestamp) into installed SKILL.md frontmatter

### Session 19:33 (wai repo — managed block, docs tickets, bd fix)
- Updated managed block context pressure wording: "stop and tell the user — responses degrade past this point" (commit `238497c`)
- Created documentation onboarding epic (`wai-54xo`) with 5 sub-tasks: rewrite introduction ([A] `wai-3vno`), add Why? to concept docs ([B] `wai-rt31`), slim README ([C] `wai-79mc`), expand design principles ([D] `wai-16dj`), annotate quick start ([E] `wai-ogf9`)
- Ran `/issue-review` (Rule of 5) on new tickets: found inverted dependency direction, overlap between [A] and [D], missing keep/remove list in [C]; fixed all
- Fixed beads dolt server failures — three separate issues:
  1. **Corrupted journal**: `.beads/dolt/wai/.dolt/noms/vvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvv` had bad record at offset 17,956,304. Fix: `truncate -s 17956304 <journal-file>` (backup first with `.bak`)
  2. **Zombie dolt processes**: server mode auto-starts `dolt sql-server` but never cleans up on exit. Stale `.beads/dolt-server.port` and `.beads/dolt/.dolt/sql-server.info` files cause reconnection failures. Fix: `kill <pid>` + `rm -f .beads/dolt-server.port .beads/dolt/.dolt/sql-server.info .beads/dolt/.dolt/noms/LOCK`
  3. **Root cause fix**: switched `.beads/metadata.json` from `"dolt_mode": "server"` to `"dolt_mode": "embedded"` — dolt runs in-process, no separate server, no zombies, no port files
- Recovered data after embedded mode switch emptied the database: `bd import .beads/issues.jsonl` (422 issues), then fixed backup jsonl schema (int-for-bool fields: `ephemeral`, `is_template`, `pinned`, `no_history`, `archived`) and imported 23 delta issues
- Committed and pushed beads fix as `108f5e4`

#### Beads Dolt Fix Runbook (for other repos)

When `bd` fails with "Dolt server unreachable" or "database is locked":

```bash
# 1. Check for orphan dolt processes
ps aux | grep "dolt sql-server" | grep -v grep

# 2. Kill orphans (skip the global one at ~/.local/share/beads/)
kill <pid>

# 3. Clean stale state files
rm -f .beads/dolt-server.port .beads/dolt/.dolt/sql-server.info .beads/dolt/.dolt/noms/LOCK

# 4. Test
bd ready

# 5. Permanent fix: switch to embedded mode
# Edit .beads/metadata.json: "dolt_mode": "embedded"
# If database is empty after switch: bd import .beads/issues.jsonl
```

If the journal is corrupted ("invalid journal record length" in `.beads/dolt-server.log`):
```bash
# Find corruption offset in log, backup, truncate
cp .beads/dolt/wai/.dolt/noms/vvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvv{,.bak}
truncate -s <offset> .beads/dolt/wai/.dolt/noms/vvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvv
```

- **Next:** work through docs epic tickets (A, B, E are ready to start in parallel)
