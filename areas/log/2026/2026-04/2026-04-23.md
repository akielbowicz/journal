# 2026-04-23

## Focus
- paranoid NetDiag polish

## Log

### Session 10:02 (paranoid)
- Reviewed PARANOID-9vu (NetDiag: UI) — all tasks 6.1–6.8 already implemented; closed issue
- Picked up PARANOID-v6q (NetDiag: Polish) — tasks 7.1 and 7.2
- **7.1 Empty states:** added no-network check before capture with user-facing error message
- **7.2 Error handling:**
  - Added 30s overall capture timeout in `SnapshotCaptureEngine` via `withTimeoutOrNull`
  - Added transport-change detection (start vs end) with `transportChanged` flag on `CaptureResult.Success`; surfaces Toast warning in `NetDiagActivity`
  - Wired up `SnapshotFileExchange.importFromUri` via `ActivityResultContracts.GetContent` file picker
  - Added "Import from file…" option in compare-with-saved dialog (both empty and populated states)
  - Invalid JSON handled by existing `SnapshotFileExchange` error paths
- Build + tests pass; committed and pushed as `8748b72`
- Closed PARANOID-v6q — `add-netdiag-app` spec now 44/44 tasks complete
- **Next:** archive `add-netdiag-app` change; remaining open issues are P3 instrumentation tests (PARANOID-mup, PARANOID-wbv)

### Session 15:15 (ruta)
- Completed final task for `add-mode-aware-command-disclosure` spec: ruta-3ia.6 (REFACTOR)
- Structural dedup in `extensions/tutorial.ts`: `reimplement` branch of `modeTutorial` now spreads `tutorialUtilityCommands('reimplement')` instead of inlining `/ruta-why` and `/ruta-tutorial` items
- Extended `tutorialUtilityCommands` to accept mode context for reimplement-specific `/ruta-why` copy (`'explain the guardrails for this mode'`)
- Copy fix: `/ruta-scope` purpose changed from adverb-first to verb-first phrasing
- All 95 tests pass; issues closed and pushed
- `add-mode-aware-command-disclosure` is now 29/29 complete
- **Next:** begin `add-session-per-spec-layout` (0/25, proposal and specs already exist as untracked files)

### Session 17:51 (atril)
- Implemented beads viewer: issue list/detail view replacing dead-end placeholder
- Created 9 tickets from beads-viewer spec (atril-9ie through atril-w01) with dependency graph
- Closed 8 tickets: data loading, list/detail, filtering, search, shareable URLs, freshness indicator, cross-refs, original placeholder bug
- New files: `beads-loader.ts` (JSONL parser + branch fallback), `beads-renderer.ts` (pure HTML render + filter logic)
- Modified: `main.ts` (async beads view, filter/search handlers), `router.ts` (added `list` mode as default), `styles.css` (list/detail + toolbar + responsive layout)
- Ran Rule of 5 review — found and fixed 3 issues:
  - Search input losing focus on re-render (cursor position restore)
  - Filter state persisting across repo switches (reset on load/switch)
  - Non-404 errors silently swallowed during branch fallback (propagate rate-limit etc.)
- UX tested via Playwright MCP: list, detail, filtering, search, dark mode, mobile (375px), back navigation all pass
- Found and fixed stale 404 browser cache bug: added `cache: "no-cache"` to `getFileContent` raw fetches
- Pushed `beads-sync` branch with `issues.jsonl` for testing; all 3 commits pushed to main
- 201 tests passing (37 new), build clean
- **Next:** atril-bbm (interactive dependency graph) and atril-70w (focused dependency view with real data) remain

## Tasks created

## Decisions
- 30s timeout chosen as reasonable ceiling for concurrent collectors (individual collectors already have 3–10s timeouts)
- Transport change shown as non-blocking Toast rather than error — capture data is still valid, just potentially mixed
- Beads viewer defaults to `list` mode (not `graph`) — list is immediately useful, graph is a future ticket
- `cache: "no-cache"` on raw.githubusercontent.com fetches — 404s were cached for 5min, poisoning branch fallback
