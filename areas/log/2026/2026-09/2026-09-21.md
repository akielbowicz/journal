# 2026-09-21

## Log

### Session:09:55 (pretender — usage/value investigation → findings-to-fixes loop: 5bw + 68j merged)
- **Value investigation** (via /renew): mined pi session history (~989 session files) — `pretender check` invoked 2,701× across 156 sessions in ≥10 repos (Testimonial.jl, testaruda, genesis, wai, Tray.jl, cositos, REPLy.jl…); genesis + Testimonial.jl wire it into commit hooks. **But zero findings ever converted to fixes**: 15+ violation tool-results, 0 in-session edits; genesis has 13 files flagged ≥3 weeks unresolved. Diagnosis: advisory exit-0 + noise + no persistence = agents learn to ignore.
- **Issue-review (Rule of 5)** over the new ticket set: caught refuted root-cause hypothesis on 5bw (TOML patterns exist; the capture path was broken), missing dep edge dj5→u8a, gameable >80% acceptance (added anti-goal), unverifiable HITL acceptance on 15w (replaced with runnable meter), all tickets missing metadata.files/base_commit. All fixed in-tracker.
- **pretender-5bw** (PR #16, `6f4d229`): root cause = Rust & Julia `metrics.scm` had NO `@assert.*` captures (only Python). Added `@assert.macro_invocation` (Rust: assert/assert_eq/assert_ne/debug_assert*/panic/unreachable) + `@assert.macrocall` (Julia: @test/@test_throws/@test_broken/@test_logs/@assert). TDD red→green; suite 321; 0 false reds on genuine tests.
- **pretender-68j** (PR #17, `f17a01d` + proposal `52b7f11`): openspec `update-min-assertions-scope` — rule now applies only to test-identifying units (Rust `#[test]` via preceding-sibling attribute scan, or name pattern `^test_`, `_test$`, `^test[A-Z]`, `Test$`, `^test$`); fixtures/helpers exempt, all other rules unchanged. Anti-goal verified: config TOMLs byte-identical. **Meter: min_assertions self-scan events 236 → 0.** Dogfood fixes surfaced by the now-trustworthy rule: 6 genesis compile-smoke tests got real assertions (scaffold run in fixture, discovery register in fixture root, URL reduction, block content), `setup_duration_test` helper renamed `duration_setup`. 321 green, CI green both PRs, merged, bd closed + exported.
- **Gotchas**: `.beads/embeddeddolt` + `no-db: true` — `bd dolt push` is a no-op that errors (worktree git bug upstream); real sync = committed `issues.jsonl` (needs `git add -f` after bd export). PATH-installed `pretender` (~/.cargo/bin, Jul 28) is stale — refresh with `cargo install --path pretender`.
- **Next:** queue is `15w` (gate exit-code enforcement — now trustworthy) + `u8a` (stable finding IDs → resolution rate; makes the <5% kill-criterion metric computable) → `dj5` (findings→bd tickets) behind u8a. Consider bd dolt remote removal to silence the push error.
