# 2026-09-03

## Focus
- phormasci.github.io: convert runtime markdown loading to build-time static generation + CI integration

## Log

### Session:17:52 (phormasci.github.io)
- Replaced runtime content loading with build-time rendering: deleted `js/content-loader.js`, added zero-dep `scripts/build.js` (auto-discovers `content/**.md` per language, sorts by frontmatter `order`, injects semantic HTML between `<!-- build:start/end -->` markers, generates schema.org JSON-LD for services/trainees/team)
- Regenerated all 10 pages (en/es) with static accessible markup: `<article>` cards, h1→h2→h3, anchor IDs from titles, HTML-escaped text (allowlisted `br`/`strong` for mission hero)
- Rule of 5 review surfaced CI integration gaps → added `.github/workflows/build-check.yml` (PRs touching content must commit regenerated HTML via build + `git diff --exit-code`), build steps in `static.yml` and `pr-preview.yml` (+ `scripts/build.js` path trigger)
- Added `just check` recipe (mirrors CI), fixed broken `just validate` (graceful tidy skip), documented build system in README/AGENTS.md/CLAUDE.md
- PR #22 created, merged to main; `build-check` ✅; production phorma.sh verified live: 9 service cards + 13 trainee cards + JSON-LD all served statically, zero runtime loader references
- Diagnosed deployment topology: GH Actions deploys to GitHub Pages, but phorma.sh DNS → Cloudflare Pages (`phorma-sh` project) which only updates via manual `just deploy` (wrangler). User ran it manually; production verified correct.
- **Next:** deploy-preview workflow still broken since 2025-12 (expired `PREVIEW_REPO_TOKEN` for `pr-site-preview` repo); consider Cloudflare auto-deploy via `wrangler-action` + `CLOUDFLARE_API_TOKEN` in `static.yml` so merges ship to production directly

## Tasks created

## Decisions
- Build-time rendering over SSGs: kept zero-dependency static architecture (marker-based injection into committed HTML) instead of introducing a framework — matches site philosophy
- Auto-discovery of markdown files by `order` frontmatter instead of hardcoded file lists
- JSON-LD emitted inline per page (services/courses/team) for machine parseability
