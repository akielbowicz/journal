# 2026-09-03

## Log

### Session: 17:02 (cositos — membrane blog-series investigation → docs change, shipped)

- **Repo**: `~/para/areas/dev/gh/sk/cositos`
- Read all four posts of phronemophobic's "How to build a functional UI library from scratch" series (what-is-a-user-interface, ui-model, reusable-ui-components, html-tax) — the piece the earlier membrane repo-level memo (2026-08-28) had missed
- Wrote grounded research doc `.wai/projects/investigations/research/2026-09-03-membrane-blog-series-functional-ui.md` — findings F1–F6, each verified file:line against the implementation; `rule-of-5-universal` review caught 3 wrong citations (lifecycle.py:35–38→43–45, :135–156→147–166, model.js:64–67→60–63) before they shipped
- Pre-planning questions resolved by grounding: Q1 — contrib/ uses zero custom messages; examples use `{event: ...}` dicts, so the intent convention was reshaped from tuples to the dict family. Q2 — private-state key mechanically safe (SendState include filtering, verbatim dump_model) but must NOT be `_`-prefixed (`_esm`/`_css`/`_model_*` collisions) and export behavior is a product decision. Q3 — both conventions are protocol-opaque, zero machinery; one external check remains (stock AnyModel unknown-key tolerance) → beads `cositos-40e`
- OpenSpec change `functional-ui-docs` (proposal/design/specs/tasks, validate ✓) + second ro5 review (fixed: `just`→`mise run docs`, unverifiable conditional `ah check` task, added glossary task)
- Applied the change: architecture.qmd (event→intent→effect vocabulary, membrane as 3rd convergent citation, UI-IR criteria checklist), state-discipline.qmd (parent-in-charge lineage), authoring-widgets.qmd (intent-shaped `msg:custom` convention section), glossary.qmd (intent/effect entries). Docs-only, +233/−1
- **Committed** `82fe4f5` (staged by path, no `-A`); **pushed** per user authorization
- **Next:** task 4.1 `mise run docs` render check (quarto not installed here — structural anchor checks passed instead); `cositos-40e` external verification unblocks the F4 private-state-key proposal

## Tasks created

- `cositos-40e` (P2): verify stock anywidget AnyModel tolerance of unknown/undeclared state keys on comm_open/update — gates F4 private-state-key proposal

## Decisions
- Intent convention extends the existing `{event, ...}` dict family (evidence: examples/widgets/button.js, download_button.js) — NOT membrane's `[type, path, value]` tuples
- No renaming of lifecycle effects to "commands" — churn across 4 language ports for zero behavior gain; vocabulary distinction lives in docs
- F4 private-state key excluded from the docs change; gated on cositos-40e + export-behavior decision
- `_`-prefixed private-state keys rejected (ecosystem semantics: `_esm`, `_css`, `_model_*`); plain namespaced key like `"ui_state"` if pursued
