# 2026-09-17

## Log

### Session:12:13 (insta — repo bootstrap → grill-me → OpenSpec proposal → multi-agent spec evaluation → tracer-bullet tickets → close)
- Bootstrapped empty `lit/insta` repo with ddl: `ddl init` (wai + incitaciones skills), turu init + sync (whisper slots `local/insta`, repo env.md seeded), dont init, git init; configured openspec + beads (installed but unconfigured) — AGENTS.md now carries OpenSpec+WAI+TURU+beads blocks
- Imported the concursos-literarios spec as wai research into project `insta-scraper`; ran grill-me (12 questions, one per turn) resolving the full design: purpose-built collector with agent-owned IG account (not Apify-style scraping), instagrapi+Playwright dual transport, 3 intake channels (CLI/DM/mentions) with pending→tracked lifecycle, per-source cursors (not the algorithmic feed), manual-first auth, jittered session budgets, SQLite store, one-export-three-consumers publication, content-only public boundary, fail-fast + human-approved browser reads, notifications-only comment coverage
- Scaffolded openspec change `add-instagram-source-collector` (proposal/design/tasks + 4 spec deltas); fixed validator quirk (requirement must lead with "The system SHALL…")
- Rule-of-5 review round 1 (NEEDS_REVISION): export data-contract gap (normalized fields = parent normalizer's job → v1 = raw attributed records), silent 36h-coverage degradation, cursor atomicity on partial fetch, bounded retry rule
- Ran 7 parallel headless `pi -p` sessions (spec-eval, testability, edge-case, pre-mortem, red-team, adversarial-stakeholder, error-handling diagnostics) — outputs in /tmp/spec-evals/; applied all converged findings: intake abuse caps (≤3/event, ≤8/day, mention-follows ≤2/day), 36h as nominal invariant + scaling ceiling + degradation paging, N=12 refetch window for edit/deletion detection, insertion-order cursor HWM, versioned event envelope (post_id, version), paging ack + dead-man's switch, excerpt ≤280 graphemes, HTML/RSS escaping, atomic export, transient-vs-terminal failure classification, idempotent follow via friendship read-back, handle normalization, cross-channel dedup, 14-day pending age-out
- Rule-of-5 round 2 on the fixes (READY): caught duplicate task 3.5, rebuildability-vs-frozen-history contradiction, pending-backlog aging; user resolved 4 open decisions: standing browser pre-auth KEPT, mute semantics + excerpt confirmed, pager channel DEFERRED to deploy config (recorded in design.md Review Resolutions)
- Created 30 beads issues (insta-*) with 48 dep links mirroring tasks.md P1–P5; ran issue-review skill (5 passes): metadata.files + acceptance criteria set on all 31, co-mod notes on shared files, created tracer bullet insta-2j5 (T0: one source end-to-end) fixing the horizontal-layer-cake finding; verdict READY. Note: base_commit anchors pending first commit
- **Next:** `bd ready` → insta-yuv (P1.1 scaffold Python package + CLI skeleton); after first commit, anchor base_commit on all 31 tickets; then T0 tracer bullet insta-2j5 (after 1.3 + 2.1)

### Session:12:58 (genesis — feedback feature investigation → cross-repo ticket filing → close)
- Confirmed genesis has a user-facing issue-reporting feature: `src/feedback.rs` (`handle_feedback`), composing scratch/context/redactor/gh submodules, with `--dry-run` + `--from-last-error`
- Audited all 8 genesis dependents in charly-vibes: **wired** = wai (deepest, own command module + fallback ladder), dont, espectacular, pretender (cleanest one-liner pattern), vampiro (with genesis_compatibility.rs test); **missing** = testaruda (uses guide+config), whisper (guide only), dulce-de-leche (cli+envelope only)
- Filed P2 tickets in each repo's own beads DB: testaruda-6k0z, whisper-1ug, DDL-5db (prettier pattern: `handle_feedback` delegation + compat test); closed the 3 misplaced genesis-* tickets with pointer comments
- Noted: genesis beads DB has no dolt sync remote configured (`bd dolt push` only prints remote help)
- **Next:** when picking up any of the three tools, `bd ready` in that repo and claim the feedback-wiring ticket

### Session:15:14 (REPLy.jl — Julia 1.13 + 1.10 CI support → CI green → close)
- Verified REPLy on Julia 1.13.0 (installed at ~/.local/opt/julia-1.13.0): full suite (~5M assertions), smoke test, and docs build all pass; `julia = "1.10"` compat already covered it
- CI: `check` job now a version matrix (1.10/1.12/1.13, fail-fast off), coverage+Codecov gated to the 1.12 leg; docs.yml bumped to 1.13; excluded untracked `aek-analysis/` in _typos.toml (was blocking all commits via typos pre-commit)
- Fixed test fragility: build_test only rebuilt the replyc launcher when the file was missing — now also rebuilds when built by a different Julia version or its pinned `--project` scratch dir is gone; self-healed the user's broken `~/.julia/bin/replyc` launcher
- Fixed CI breakage: lefthook@latest couldn't compile on runners (go-json-experiment dep vs stable Go) → dropped lefthook from CI (unused there) and pinned actionlint v1.7.12 (`@latest` main carries the same broken dep)
- De-flaked concurrent-ephemeral-evals timing test: threshold equalled the serial time (zero margin); now sleep 0.5s, threshold 2×sleep_s, 3-attempt retry
- Investigated the 1.10 compat floor (AEK audit flagged it UNVERIFIED): failures were PrecompileTools 1.3.x on a 1.12-resolved manifest (needs Base.StaticData; fresh resolve picks 1.2.x), 1.10 Pkg "can not merge projects" from local test/Manifest.toml, and three test-only incompatibilities (isexecutable shim, Revise mock const injection, clone const-reassignment silently ignored on 1.10 per #56933) — no src changes needed; suite now passes on 1.10.12/1.12.5/1.13.0
- CI fully green on all three legs (14m run); durable knowledge routed to ~/.whisper/repos/sk/sashakile/REPLy.jl/env.md (multi-version testing gotchas, CI pins, launcher self-heal)
- **Next:** nothing pending in REPLy.jl — `bd ready` for open backlog (P0 eval-boundary tickets etc.); watch the upstream go-json-experiment/Go breakage when bumping actionlint

### Session:15:14 (network — gnu.org unreachable diagnosis → IP block at FSF side → close)
- Diagnosed gnu.org unreachable: DNS fine (local + Google DoH both → 209.51.188.116, no poisoning); IPv6 unusable locally (no v6 route, incidental); IPv4 TCP 80/443 and ICMP to 209.51.188.116 all silently dropped, while internet overall is fine (8.8.8.8 ~12ms, example.com/kernel.org OK)
- Same-subnet control passed: fsf.org (209.51.188.174, same /24 + same datacenter) responds instantly — ruled out ISP-wide outage or local firewall (no nftables/iptables blocks, no VPN/proxy, no unusual routes)
- Traceroutes identical through hop 12 (FSF border 206.53.143.61); fsf.org answers at hop 13, gnu.org goes silent — filter at the FSF/GNU side drops packets sourced from user's IP
- Phone on mobile data (different public IP) loads gnu.org fine → block is pinned to public IP 186.148.206.6 (Telecom Argentina, AR); most likely on FSF/GNU anti-DDoS/abuse blocklist (consumer ISP range)
- **Next:** email sysadmins@gnu.org asking to remove the block (draft prepared: IP + evidence, both addresses, traceroute endpoint); interim workaround = VPN/proxy/Tor for different source IP; retest with a TCP connect to 209.51.188.116:443 after they reply

### Session:16:17 (genesis — add-aix-eval-loop full implementation → v0.7.0 release → downstream notification → spec deployment)
- Resumed genesis, set up a repeatable per-ticket pipeline (claim → RED from spec scenarios → GREEN → TIDY → docs/gates → Rule-of-5 → close/push), recorded via `bd remember`; then ran it end-to-end on all 5 add-aix-eval-loop tickets:
- §1 genesis-f2o: envelope receipt metadata — `TerminalOutcome` + `ReceiptMeta` + `with_receipt()`, byte-identical golden-file pin (tests/golden/), exhaustive round-trip; §2 genesis-35d: `estimate_token_cost` (chars/4, ±25% band) + `*_bounded` generators with 4-stage deterministic degradation, budget-relative test assertions; §3 genesis-lzo: `distractor_file()` bait fixtures + `doc_drift_blindness()` check (new `ERR_DOC_DRIFT_BLINDNESS`), word-prefix bait matching (Ro5 catch), `ScenarioReport.model` + JSON serialization; §4 genesis-m3p: `from_feedback_context`/`from_last_error` regression-scenario conversion, tool-fault (not agent-fault) classification, in-process guarantee; §5 genesis-g5g: wrap-up gates, no structural clones
- v0.7.0 released (HITL-authorized): evals how-to updated (new Step 7 distractors/doc-drift + stale status fix), version pins bumped (doc_sync guard), CHANGELOG dated (EXCL-002 resolved: dated headings, fresh Unreleased), tagged v0.7.0 → crates.io published green
- Downstream notification two-layer: Dependabot config seeded + pushed into all 7 downstream repos (cargo semver `"0.6"` excludes 0.7.0 → auto bump-PRs); new `just notify-downstream <ver>` + `just seed-dependabot` recipes; ran notify — all 7 repos got announcement issues
- genesis-ntg reconciled: dont-bpuo already CLOSED (repo-local ADR + follow-ups in own repos), wai-0qx0/pretender-blf VP tickets closed; epic note updated and pushed
- Openspec: archived add-aix-eval-loop + add-genesis-foundation (28/29→29/29, genesis-9o5 verified) — push blocked by 28 espectacular no-toml findings: first scenario-bearing specs ever deployed need contracts at `.espectacular/<spec>/<scenario-id>.toml` mapped to real test names; created all 28, learned the deployed-vs-overlay lifecycle (gotcha recorded via `bd remember`); `ah check` 0 findings, CI green, pushed cfe67f4
- **Left:** genesis repo itself is clear; remaining work lives downstream — genesis-dvf (testaruda feedback wiring), genesis-ntg epic remainder (5-tool VP upgrades + bpuo wiring chores in their repos), genesis-le7 deferred
- **Next:** `/clear` done via close; next session `/renew genesis` → likely genesis-dvf or the ntg downstream breakdown
