# 2026-08-27

## Log

### Session:16:52 (titi — status report, integration CI fix, follow-up tickets)

- **Repo**: `~/para/areas/dev/gh/sk/titi`
- **Project status report** (`project-status-report` skill): overall Amber — v0.3.0/0.3.1 shipped & en NuGet, pero Integration Tests workflow en rojo 100% de runs desde 2026-07-30 (~29 fallos consecutivos)
- **Fixed integration CI** (bd `titi-9rf`, closed) — tres causas en capas:
  1. `integration.yml` restauraba `src/` con `--locked-mode`, pero `PublishAot=true` inyecta paquetes AOT flotantes que derivan con el SDK → NU1004 en cada run. Alineado con la estrategia ya probada en `ci.yml` + pre-push hook (`--force-evaluate` para src, `--locked-mode` para tests) — commit `32f0a1f`
  2. `ParseCobertura` ignoraba el `<sources>` root embebido de coverlet (los filenames son relativos a ese root, no al repo) → paths de edges mal formados (sin `libs/`) → `static-deps` devolvía `affected_tests: []` siempre. Fix: probar roots embebidos primero, caller root como fallback — commit `bbb6e85`
  3. `SyntheticFixtureTests` asertaba el archivo `fingerprint` eliminado desde TID-11 → actualizado a `edges/project-fingerprints.json`
- **Causa raíz del silencio**: el pre-push hook excluye `Category=Integration`, así que la suite de integración nunca se validaba localmente y el rojo en CI senormalizó
- **Verificación**: 434 unit + 6/6 integration en local (estable en runs repetidos), workflow_dispatch run 33109593013 — **primer success del workflow en su historia**, `ci.yml` verde en push
- **Tickets creados**: `titi-6xz` (bug P2: aislamiento de cache `.titi` compartido entre clases de test — order-dependent), `titi-kug` (chore P3: warnings de xUnit analyzers), `titi-rq1` (task P3: decidir política del lock file de src, ahora advisory) — los dos primeros linkeados discovered-from `titi-9rf`
- **Next**:
  - `titi-6xz` es el candidato a agarrar primero (flake class real)
  - Considerar correr la suite de integración también en el pre-push hook si el costo de tiempo lo permite
