# 2026-08-31

## Log

### Session: 18:04 (cositos — membrane & plotje research)

- **Repo**: `~/para/areas/dev/gh/sk/cositos`
- **Investigated phronmophobic/membrane** as an architectural reference
  - Same pattern as cositos: platform-agnostic data model + minimal per-platform seam ("to run on a platform: drawing primitives + event loop")
  - Independent validation for cositos' effects-returning lifecycle reducer (membrane.component handlers are pure, return effects)
  - Flagged opportunity: a **MembraneChannel** — render cositos widget state as membrane UI values → live widgets in desktop/terminal, outside Jupyter, zero new JS
- **Investigated scicloj/plotje** (Grammar of Graphics for Clay)
  - **Key discovery: Plotje is built ON membrane** (`impl/membrane.clj` compiles plans → membrane UI components) — three-way stack: Plotje (spec) → Membrane (render/events) → cositos (two-way ClayChannel)
  - Today Plotje-in-Clay is static SVG (one-way); cositos is the missing two-way piece
  - Integration path 1 (recommended, demo-scale): SVG-embed widget + pointer events over `ClayChannel`; pixel→data inversion free via **wadogo** invertible scales (`impl/scale.clj`)
  - Path 2 (bigger lift): interpret Plotje's serializable malli `plan` schema client-side — vega-lite-style; wait for schema stability (alpha)
- **Recorded both investigations in wai**
  - Created `.wai` project `investigations` (no active project existed since `cositos-core` was archived)
  - `.wai/projects/investigations/research/2026-08-28-what-cositos-can-learn-from-phronmophobic-membra.md`
  - `.wai/projects/investigations/research/2026-08-28-plotje-cositos-a-path-to-interactive-plotting.md` (cross-linked to the membrane one)
- Ran `dont prime --json` and `wai status` at start; `wai search` verified both artifacts findable
- **Next:** optionally file a beads issue for the path-1 Plotje/Clay interactive demo, and/or the MembraneChannel idea

## Notes
- wadogo (`wadogo.scale`) has invertible :linear/:log/:categorical scales — this unblocks the pixel→data mapping in the Plotje widget demo
- Plotje is 🛠 alpha: path 1 insulates cositos from plan-schema churn; path 2 should wait
