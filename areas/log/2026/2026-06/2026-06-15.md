# 2026-06-15

## Focus
- pretender hooks install/uninstall

## Log

### Session 10:45 (pretender)
- Resumed pretender project; `pretender-hay` (P1 native pre-commit hook generator) was next
- Core implementation already existed: `install_pre_commit_hook` / `uninstall_pre_commit_hook` in `main.rs`, shim using `--staged` (from completed `pretender-a80`)
- Ran Rule-of-5-Universal review; found one HIGH issue: `install` silently overwrote foreign hooks (asymmetric with `uninstall`'s safety check)
- Fixes applied:
  - `install_pre_commit_hook`: added existence + marker check; refuses to overwrite foreign hooks with clear error
  - `pre_commit_hook_contents()`: refactored from `&'static str` to `String` built from `PRE_COMMIT_HOOK_MARKER` constant — marker and shim content now definitionally linked, cannot drift
  - Shebang updated to `#!/usr/bin/env sh` for portability
  - Init interactive test assertion tightened to verify `--staged` flag
- Tests: grew from 1 → 6 hooks tests (round-trip, dir-creation, idempotent reinstall, noop uninstall, install refuses foreign hook, uninstall refuses foreign hook)
- All 101 tests pass; pushed to remote
- **Next:** `pretender-9hk` — External metric plugin wrappers (P3, eslint/ruff/clippy/staticcheck)

## Tasks created

## Decisions
- install guards against foreign hooks: mirrors uninstall's existing safety behavior
- shim built from constant: prevents marker/content drift
