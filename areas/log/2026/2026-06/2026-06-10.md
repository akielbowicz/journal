# 2026-06-10

## Focus
- Extracción y documentación de datos de uso de IA para serie de posts

## Log

### Session 17:42 (microdancing)
- Creé branch `data/datos-uso-ia` con toda la data extraída
- Construí `scripts/usage-tracker.py` v3 que extrae datos de Claude, Pi, Amp con filtro charly-only
- Extraídos **81,904 interacciones** de enero 11 a junio 10
- Integré datos de Amp (@ampcode/cli, agente autónomo) — 225 tareas charly, cubre enero-febrero
- Confirmadas fechas de suscripción Claude: Pro $20 (mar 19 → abr 19), Max $100 (abr 19 → jun 19), Pro $20 (jun 19 →)
- Costo real estimado: **$65.70** (Codex pay-per-token + OpenRouter, el resto cubierto por suscripción)
- Analizadas **1,884 sesiones** Claude: 48% cortas (1-10 turns), 18% autónomas (con Agent)
- Skills: rule-of-5-universal lidera (115 usos), comandos: /clear (307)
- Boceto de serie de posts actualizado en drafts/
- Gap enero 1-10 documentado como información no disponible
- **Next:** generar dashboard HTML con Chart.js, empezar a escribir post principal

### Sessions 19:38 (pretender — 2 sessions)
- **Session 1 (19:09):** Implemented `pretender-zsu` (P2) — history feedback loop for `cognitive_max` violations; new `src/history.rs` with `ViolationEvent`, `EventStore` (atomic tmp→rename writes, age+size pruning), `compute_summary` (hotspot/pattern recurrence); 11 unit tests; wired into `pretender check` via `emit_history_events`; recurrence hints printed in human output
- **Session 2 (19:30):** Implemented `pretender-a80` (P3) — diff-only mode via `git2`; new `src/git.rs` with `staged_files()` (index vs HEAD diff, empty-repo safe) and `diff_base_files()` (tree-to-tree diff); `apply_file_filter()` with canonical path matching; empty-staging short-circuit skips full tree walk; `--staged`/`--diff-only` mutual exclusion via clap `conflicts_with`; pre-commit hook template updated to use `--staged`; 4 new integration tests
- Both sessions ran `rule-of-5-universal` review — issues fixed before commit (CORR: non-atomic write, fingerprint `./` split, mod ordering, clap conflicts, cwd allocation)
- **Arc:** Two P2/P3 issues shipped back-to-back; pre-commit hook now fully functional with proper scope (scans only staged files, not the whole tree); 96 tests passing; pushed as `83da068` + `5283e43`
- **Next:** `bd ready` shows `pretender-b5o` (P1 wave) and `pretender-9hk` (external metric plugin wrappers, P3) as next candidates

### Sessions 19:39 (espectacular — 3 sessions)
- **Session 1 (18:44):** Rust `CargoAdapter` + TypeScript `VitestAdapter` in `src/adapters/`; wired into dispatch; closed `6nm.4/5/6`; 175 tests → pushed `55f59c9`
- **Session 2 (19:19):** `src/explain.rs` — 31-topic compile-enforced registry (`--json`, `--list`, did-you-mean); 14 unit + 11 CLI tests; closed `6nm.10`; 200 tests → pushed `704444e`
- **Session 3 (19:39):** Tracer bullet `e5w` — full dont→espectacular→wai drift signal pipeline across 3 repos: `dont` emits `.dont/events/rejection-*.json` when `DONT_EMIT_EVENTS=1` (new `src/events.rs`); `ah signals` reads them and returns `DriftSignal[]` JSON (new `src/signals.rs`); `wai doctor` surfaces signals as Warn when `WAI_DONT_SIGNALS=1` (new `drift_signals_to_check_results`); 19 new tests across 3 repos; all three repos pushed
- **Arc:** `add-quality-measurement-and-adapters` sprint: pytest/Rust/TS adapters + explain subcommand + end-to-end feedback loop proof-of-concept all shipped in one day; spec completeness 44→~50/82
- **Next:** `espectacular-6nm.12` (P1 — integration tests + openspec validate --strict) or `espectacular-6nm.7` (P2 — custom runner plugin protocol)

## Tasks created
- [ ] Generar dashboard HTML con Chart.js para visualizar datos hora a hora
- [ ] Escribir post principal de la serie con datos reales

## Decisions
- Gap de enero 1-10 se deja como información no disponible en vez de forzar una explicación
- Se usó Amp como placeholder para enero-febrero (no tiene costo ni modelo, solo cambios de archivo)
- Branch `data/datos-uso-ia` queda como fuente de verdad de los datos extraídos