# 2026-06-03

## Focus
- extend llm-dashboard to multiple AI tool sources

## Log

### Session 10:22 (canticos)
- Extended `bin/claude-dashboard.py` into a multi-source `bin/llm-dashboard.py`
- Added source adapters for: Claude Code (`~/.claude`), Pi (`~/.pi/agent/sessions`), Amp (`~/.amp/file-changes`), Gemini (`~/.gemini`)
- Designed normalized schema: `SessionSummary`, `TurnSummary`, `EventSummary`, `ErrorSummary`
- Pi adapter: full usage + cost support (rich structured JSONL logs)
- Amp adapter: activity-only (file changes, timestamps, project inference from git roots)
- Gemini adapter: activity-only (tmp mtime, `.project_root`, shell history)
- Renamed entrypoint to `bin/llm-dashboard`, kept `bin/claude-dashboard` as compatibility alias
- Updated `install`, `README.md`, `docs/index.html`, `CHANGELOG.md`
- Committed and pushed to `main` (commit `1a78e31`)
- **Next:** label sources as usage-capable vs activity-only in the UI; investigate Gemini structured logs if they become available

## Tasks created

## Decisions
- Gemini support is activity-only until structured session/usage logs are found
- Amp support is activity-only (no token/cost data visible)
- Pi is first-class alongside Claude (rich structured logs, provider/model/cost)
