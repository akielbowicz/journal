# 2026-06-09

## Focus
- Emacs keybindings and spell check configuration

## Log

### Session 16:49 (presentations)
- Learned Doom Emacs markdown editing basics: `C-z` toggles Evil mode to Emacs state, navigation keys (C-p/n/b/f, M-b/f, C-a/e), markdown-mode commands (C-c C-l for links, C-c C-t for headings, TAB/S-TAB for folding)
- Set up Spanish spell check in Doom Emacs:
  - Enabled `(spell +flyspell)` module in `init.el`
  - Downloaded `es_ES.dic` and `es_ES.aff` from LibreOffice dictionaries to `~/.local/share/hunspell/`
  - Configured `DICPATH` environment variable and `ispell-dictionary` in `config.el`
  - Dictionary switching via `M-x ispell-change-dictionary`
- **Next:** None — session was ad-hoc Q&A, no pending items

## Tasks created

## Decisions
- Installed Spanish dictionary via direct download (no rpm-ostree/reboot needed)

### Session 19:40 (general)
- Investigated ESP32-C3 USB connection issue from 2026-06-06 log
- Searched Universal Blue Discourse + ublue-os GitHub — no prior ESP32-related issues found
- Found esp-idf #9657 (Ubuntu), similar symptom: ESP32-C3 USB detected but missing JTAG; resolved via udev/forum workaround
- Checked kernel: `cdc_acm` driver is loaded, matches CDC-ACM interfaces by class/subclass/protocol
- Kernel has `esp32s3-acm` DT binding but that's device-side (gadget), not host-side
- No kernel patches exist for ESP32-C3 USB on linux-usb mailing list
- User connected ESP32-C3 — `cdc_acm` DOES bind and creates `/dev/ttyACM0`, but device brownout-cycles every ~3s (same as ESP8266 in the earlier session)
- Changed USB ports — same cycling; ruled out specific port issue
- Discussed power: 3.7V 18650 won't work (too low for USB 5V regulator, too high for 3.3V pin at full charge); needs external 5V (power bank, phone charger)
- User needs `dialout` group membership for device access
- **Arc:** Confirmed there is no missing kernel driver — `cdc_acm` handles the ESP32-C3 USB Serial/JTAG controller natively via standard CDC-ACM interface matching (VID 0x303a, PID 0x1001). The actual problem is brownout, same as the ESP8266.
- **Next:** Test ESP32-C3 with external 5V power (power bank or phone charger) to confirm brownout stops; add user to `dialout` group for permissions
