# 2026-06-17

## Focus
- pretender: implement mutation testing wrapper (pretender-238)

## Log

### Sessions 18:47 (pretender — 1 session, backfilled from 2026-06-16)
- Implemented `pretender duplication` command (pretender-xgn): within-file structural clone detection via normalised AST subtree hashing
- New `duplication.rs` module; normalises identifiers→`$VAR`, literals→`$LIT_<type>`; groups subtrees ≥10 nodes by hash
- Added `tests/fixtures/python_duplicates.py`; 5 unit tests + CLI integration test; all 116 tests green
- Wired `--min-nodes` and `--cross-file` flags; committed and pushed to `main`
- **Arc:** duplication detection fully shipped; 2 issues remained open at session close

### Session (pretender — today)
- Implemented `pretender mutation` command (pretender-238): mutation testing wrapper for mutmut/cargo-mutants/stryker/pitest
- New `mutation.rs` module — language detection, output parsers for all 4 tools (pure functions), tree-sitter dry-run enumeration for non-cargo-mutants languages
- `--dry-run`: lists planned mutation sites (cargo mutants --list for Rust, tree-sitter operator/return scan for others)
- `--score-min 60` threshold, `--format json` output; survivors sorted by file/line
- 31 new unit tests + 1 CLI integration test; 149 total, all green; pushed to main
- **Arc:** mutation command fully implemented and pushed; only `pretender-vuc` (explain metric) remains open

## Tasks created

## Decisions
- Dry-run for non-Rust tools uses tree-sitter operator enumeration (mutmut/stryker don't support --list natively)
- Stryker report parser skips NoCoverage/CompileError/Ignored mutants from total count
