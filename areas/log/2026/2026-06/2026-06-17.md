# 2026-06-17

## Focus
- pretender: implement mutation testing wrapper (pretender-238)

## Log

### Sessions 18:47 (pretender — 1 session, backfilled from 2026-06-16)
- Implemented `pretender duplication` command (pretender-xgn): within-file structural clone detection via normalised AST subtree hashing
- New `duplication.rs` module; normalises identifiers→`$VAR`, literals→`$LIT_<type>`; groups subtrees ≥10 nodes by hash
- Added `tests/fixtures/python_duplicates.py`; 5 unit tests + CLI integration test; all 116 tests green
- Wired `--min-nodes` and `--cross-file` flags; committed and pushed to `main`
- **Arc:** duplication detection fully shipped; 2 issues remained open at session close

### Session (pretender — today)
- Implemented `pretender mutation` command (pretender-238): mutation testing wrapper for mutmut/cargo-mutants/stryker/pitest
- New `mutation.rs` module — language detection, output parsers for all 4 tools (pure functions), tree-sitter dry-run enumeration for non-cargo-mutants languages
- `--dry-run`: lists planned mutation sites (cargo mutants --list for Rust, tree-sitter operator/return scan for others)
- `--score-min 60` threshold, `--format json` output; survivors sorted by file/line
- 31 new unit tests + 1 CLI integration test; 149 total, all green; pushed to main
- **Arc:** mutation command fully implemented and pushed; only `pretender-vuc` (explain metric) remains open

## Tasks created

## Decisions
- Dry-run for non-Rust tools uses tree-sitter operator enumeration (mutmut/stryker don't support --list natively)
- Stryker report parser skips NoCoverage/CompileError/Ignored mutants from total count

### Sessions 15:59 (dont — 2 sessions)
- **2026-06-16:** Ran Ro5 universal review on 7 openspec specs across `add-eval-readiness` + `add-managed-agent-skills`; found 1 CRITICAL, 6 HIGH, 8 MEDIUM; fixed all 15 issues and pushed
- Resumed: validated both specs (`openspec validate --strict` — both clean), closed stale tdd-ro5 pipeline
- Completed `add-eval-readiness` loose end: updated `.dont/AGENTS.md` with `dont stats`, `dont export --eval`, `--no-persist` docs (task 5.6); marked all 15 tasks complete
- Ran full tdd-ro5 pipeline for `add-managed-agent-skills`:
  - **Plan:** `skill_pack.rs` module, `dont-grill` pack (router + 9 sub-skills under `subs/`), `HarnessConfig.managed_skill_packs`, doctor integration
  - **Red → Green:** 13 integration tests; implemented `generate_pack`, `pack_content_hash`, `disk_content_hash`, `refresh_managed_skill_packs`, `managed_skill_packs_status`, wired into `dont doctor` + `dont init`
  - **Refactor:** `PackHealth`/`PackState` typed return replaces string-sniff status derivation
  - **Ro5 review:** HIGH#1 stale-file cleanup gap (remove_dir_all before rewrite); HIGH#2 no unit tests; MEDIUM PackState missing Copy
  - **Fix:** all 3 findings addressed; 6 unit tests added to `skill_pack.rs`; 92 unit + 248 integration tests pass
- Pushed: `f04196b`, `b422b8d`, `cb239f5`, `bd8ed83`, `70addf4`
- **Arc:** both openspec changes fully implemented and Ro5-reviewed; dont-grill managed skill pack ships with `dont doctor --fix`
- **Next:** `wai reflect` (9 sessions overdue), then `add-managed-agent-skills` tasks 2.x (TDD for init/doctor paths) or pick from `bd ready`

### Sessions 21:24 (espectacular — 9 sessions)
- **Q6 custom runner:** `src/adapters/custom.rs` with envelope parsing; schema cross-reference via `$ref`; 11 unit + 3 integration tests
- **Q7 doctor detection:** `run_doctor()` detects pytest/cargo/vitest/property; `ah doctor --enable` writes config blocks; `insert_runner_entry`/`append_capability_block` helpers extracted to `init.rs`
- **Q8b/Q8c signals:** property/snapshot test dispatch in `evaluate_scope`; passing → quality findings, failing → `test-failing`
- **Q8.9–8.11:** `collect_quality_findings` returns findings + errors tuple; mutation tool non-zero exit propagates as `test-failing`; `QualityFinding::for_test_type()` constructor added
- **ETXTBSY fix:** test helpers use `/bin/sh script.sh` instead of chmod'd scripts to eliminate VFS race
- **Epic close:** `add-quality-measurement-and-adapters` 82/82 tasks; `espectacular-6nm` closed; `CHANGELOG.md` written for v0.1.0
- **Housekeeping:** README updated (`espectacular-gin`); managed blocks + `src/init.rs` template updated with all 7 commands (`espectacular-dl5`); openspec archive run — 4 specs promoted, 81 contracts stubbed
- **Docs site (current session):** scaffolded mdBook (`docs/`) + GH Actions deploy workflow; wrote full content for installation, commands, concepts pages; ran Rule of 5 review + applied all 13 findings; `wai way` audit → added `status.md` (38-scenario map), `llm.txt`, specs-in-docs CI copy step; anti-slop audit → 6 prose fixes; `llm.txt` now published to Pages root
- **Arc:** `add-quality-measurement-and-adapters` fully shipped and archived; complete docs site live on push; one HITL step remains (repo admin enables GitHub Pages)
- **Next:** `add-spec-quality-checks` (21 tasks, `ah lint` command); close `espectacular-h9a` epic once Pages is activated
