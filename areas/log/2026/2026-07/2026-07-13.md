# 2026-07-13

## Focus
- cositos CI/CD pipeline completion

## Log

### Session 09:23 (cositos — 1 session)
- **Release pipeline go-live:** Successfully published **cositos v0.1.3** to both PyPI (`cositos`) and npm (`@cositos/front`). All three release workflow jobs 🟢 green.
- **PyPI trusted publishing:** Configured PyPI trusted publisher for `sashakile/cositos` with `release.yml` workflow. PyPI publishes automatically on `git tag v*`.
- **npm publishing:** Worked through 3 iterations of the npm release workflow:
  - v0.1.0: scoped name `@cositos/front` — 404 (org didn't exist), tried `cositos-front` unscoped — 403 (token permissions), tried without `--provenance` — still 403, then user published manually
  - v0.1.2: user manually published `cositos-front` from local machine
  - v0.1.3: restored `@cositos/front` scoped name, configured npm trusted publishing, added `repository` field to `package.json` for provenance verification. All green.
- **CI fixes:** Added `repository` field to `front/package.json` (`sashakile/cositos` git URL) for npm provenance validation.
- **Files touched:** `.github/workflows/release.yml`, `front/package.json`, `pyproject.toml`
- **Next:** Future versions: `mise run bump-version X.Y.Z && git tag vX.Y.Z && git push origin main --tags`

## Tasks created

## Decisions
- Use npm trusted publishing (`--provenance`) instead of token authentication — simpler, no secret management
- Package name: `@cositos/front` (scoped under npm `@cositos` org)
- Python package: `cositos` on PyPI with trusted publishing
- Version bumped to 0.1.3 for first successful multi-platform release