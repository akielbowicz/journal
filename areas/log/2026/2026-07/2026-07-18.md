# 2026-07-18

## Log

### Session 18:45 (dont)
- Renewed context for dont project (inferred from cwd)
- Advanced wai phase from review → archive
- Bumped version 0.2.1 → 0.2.2 (patch release: internal tooling, docs, no user-facing changes)
- Updated CHANGELOG with missing 0.2.1 section and new 0.2.2 section
- Tagged v0.2.2 with annotated tag
- Pushed to remote (`main` + `v0.2.2`) and published to crates.io as `dont-cli` v0.2.2
- All 14 tests passing, build clean
- **Next:** project archived — no active work pending

### Sessions 19:10 (testimonial.jl — 2 sessions)
- Diagnosed and fixed CI failures in Testimonial.jl
- **fix: `handle_discover` Pkg.test() compat** (`src/Protocol.jl`): On Julia 1.12+, `Pkg.test()` sets `pwd()` to the `test/` subdirectory, making `joinpath(pwd(), "test")` resolve to a non-existent path. Fixed by deriving project root from `@__DIR__` instead of `pwd()`.
- **fix: missing Coverage dep** (`scripts/TestimonialRunner/Project.toml`): Coverage was removed in a cleanup commit (336c03c) but is still required by the runner subprocess. The `TestimonialRunner Project.toml has required deps` test was masked by the discover test failure.
- All 5 CI jobs now green — Julia 1.11, Julia 1.12, OpenSpec, espectacular, pretender
- **Next:** LCOV tracefile work (testimonial-7vn0 follow-up), subprocess batching optimization (testimonial-ru5t)

### Session 19:12 (testimonial.jl — testaruda-xjt.1)
- **End-to-end adapter test:** Built fixture project with @testitem blocks, tested full pipeline: discover → ingest --raw → select --files
- **fix: testaruda coverage attribution** (`testaruda/src/store.rs`): Runtime edges returned by the Julia adapter use `file:line` format, but the selection engine looks up content units by file path. Added `normalize_edge_target` to strip `:line` suffix, and made paths relative to project root. Grouped edges by normalized path to avoid SQLite NULL-duplication issue.
- **fix: Testimonial.jl `_append_runtime_edges`** (`src/Protocol.jl`): Was only emitting edges from test file lines (always empty on Julia 1.12+). Changed to iterate `coverage.source_files` map for actual source file edges.
- **fix: Testimonial.jl `_collect_coverage`** (`src/CoverageLayer.jl`): Was looking for test file in LCOV tracefile (never there). Changed to parse all source files from tracefile and return per-source-file coverage.
- **fix: Testimonial.jl `build_driver_command`** (`src/CoverageLayer.jl`): Resolved runner_dir relative to package source dir, not pwd, so adapter works from any project directory.
- **fix: Testimonial.jl driver.jl** (`scripts/TestimonialRunner/driver.jl`): Added project-under-test activation so ReTestItems finds @testitem blocks.
- **Created:** `testimonial-9se.1` (P0, coverage attribution — closed), `testaruda-xjt.1` (P1, edge-to-CU matching — closed)
- **Released:** testaruda v0.2.3 (tagged and pushed)
- **Verification:** `testaruda select --files src/Todo.jl` now returns `origin: Runtime` witnesses for tests exercising changed files
- **Next:** Integration tests (testaruda-98p), seeded-fault recall (testaruda-nut), espectacular contracts (testaruda-bad)


### Session 19:10 (pretender — major session)
- Investigated why agents don't use pretender: analyzed 566 pi sessions, configs, toolchain integration; found root causes (missing from wai way, no pi discovery, CLI friction)
- Applied Rule of 5 review to the research document
- Created 9 beads tickets (all since closed except 1)
- **pretender-1rz**: `pretender check` now defaults to current dir when no path given
- **pretender-65j**: Added `check_pretender()` to wai way + 3 new tests
- **pretender-7kj**: Added `wai way code-quality` topic guide (folded into 65j)
- **pretender-pgp**: Created `docs/agent-integration.md` with CLAUDE.md/AGENTS.md templates
- **pretender-ej9**: Measured indirect usage — 5 projects with pretender.toml, 3 with CI, 3 with hooks
- **pretender-yu7**: Defined success metrics with baselines and 2-month targets
- **pretender-chu**: Published corrected research at `docs/research/agent-adoption.md`
- **pretender-42d**: Wrote `docs/feedback-loop-design.md` — discovered feedback loop already implemented in V1
- **pretender-3nh**: Extended feedback loop to ALL metrics + JSON history in `--format json`
- Released **v0.3.0** — tag pushed, GH Actions release workflow running (binaries, crates.io, homebrew, scoop)
- **Arc**: From investigation → 9 issues → 9 fixes deployed → release. All root causes addressed: CLI fixed, wai way integration, docs created, feedback loop extended to all metrics.
- **Next**: Monitor v0.3.0 release. Only remaining ticket: `pretender-nu9` (F1 JSON envelope, blocked on dont-2j6o).
