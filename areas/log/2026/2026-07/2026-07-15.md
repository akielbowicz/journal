# 2026-07-15

## Focus
- Fix `ah check` silent exit, release v0.2.2

## Log

### Session 23:14 (espectacular)
- claimed and fixed `espectacular-vaq`: `ah check` now outputs human-readable diagnostics instead of silent JSON; `--json` flag preserved for CI/scripts
- ran Rule of 5 review on the change; fixed 3 medium issues (stdout flush before exit, section delimiters, empty-repo edge case) plus several low items
- released v0.2.2: version bump, CHANGELOG entry, all hardcoded version references updated, full CI pipeline passes (278 tests, clippy, fmt)
- pushed to main (`c89f055`)
- **Next:** `espectacular-lwl` (F1: shared JSON envelope crate) is blocked on `dont-2j6o`

## Tasks created

## Decisions
- `ah check` without `--json` now matches `ah doctor` behavior: human-readable text