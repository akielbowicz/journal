# 2026-07-09

## Focus
- pretender Windows release debugging and fix

## Log

### Session 18:35 (pretender)
- Investigated Windows release workflow failure using `gh run view --log-failed`
- Found root cause #1: `doctor.rs` uses `std::os::unix::fs::PermissionsExt` without `#[cfg(unix)]` guard — added conditional compilation
- Found root cause #2: `git2 0.19` / `libgit2-sys 0.17.0` MSVC linker missing `advapi32.lib` for 15 Windows API symbols (OpenProcessToken, CheckTokenMembership, RegOpenKeyExW, CryptAcquireContextA, etc.) — upgraded `git2` to `0.21` / `libgit2-sys` to `0.18.5`
- Verified: all 186 tests pass on Linux; re-tagged `v0.2.0` and pushed
- Release workflow re-ran: all 6 platform binaries built successfully (linux amd64/arm64, darwin amd64/arm64, windows amd64)
- Release v0.2.0 published on GitHub with all artifacts and checksums; Homebrew tap update failed (missing TAP_GITHUB_TOKEN secret — external issue)
- **Arc:** Cross-platform release pipeline now fully green
- **Next:** Configure `TAP_GITHUB_TOKEN` secret for Homebrew/scoop auto-update; continue with feature work

### Session 18:47 (charly-vibes)
- Added testaruda and espectacular (reinserted after accidental removal) to the charly-vibes profile and website project listings
- Updated `charly-vibes/README.md`, `charly-vibes/llms.txt`, `charly-vibes.github.io/index.html`, and `charly-vibes.github.io/llms.txt`
- Fixed duplicate espectacular entries across all files
- Committed and pushed both repos

## Tasks created

## Decisions

### Sessions 18:38 (elegua — 4 sessions)

**Session 1** (2026-07-08 12:02):
- Fixed F18 (CRITICAL): Wolfram oracle server auth — 127.0.0.1 bind, Bearer token auth
- Fixed F17 (CRITICAL): SympyAdapter RCE — restricted `parse_expr` namespace
- Fixed F5: `just test` bootstrap with `importorskip("sympy")`
- Fixed CI: vale-action folder scope narrowed

**Session 2** (2026-07-08 15:39):
- Fixed F12 (elegua-3v4l, P1): NaN/Inf non-finite handling in numeric comparison
- Fixed F8 (elegua-2e4f, P1): daemon-thread timeout replaced with `multiprocessing.Process`

**Session 3** (2026-07-09 18:38):
- Fixed F23 (elegua-c3p9, P2): `elegua run` CLI subcommand, `__all__` cleanup
- Fixed F21 (elegua-a89j, P2): deprecated API examples in docs to current APIs
- Fixed F19 (elegua-te5t, P2): docs/reference/index.md stub, `mkdocs build --strict` passes

**Session 4** (2026-07-09 19:00):
- Fixed F16 (elegua-bk9e, P2): snapshot key now includes `adapter_id` in canonical hash, preventing cross-adapter collisions in shared `SnapshotStore`
- `RecordingAdapter` uses inner adapter identity; `ReplayAdapter` accepts optional `original_adapter_id`
- 3 new tests (adapter-aware key behavior), all 22 snapshot tests pass; 594 total pass
- Breaking change noted in CHANGELOG.md
- Closed elegua-bk9e; committed and pushed to main

**Arc:** All 5 bugs from the audit report (F17 RCE, F18 auth, F12 NaN, F8 thread leak, F16 snapshot collision) now fixed. CRITICAL and HIGH severity items resolved.

**Next:** elegua-ouqt (matrix expansion + inheritance) or documentation tasks (elegua-27q, elegua-zw5)