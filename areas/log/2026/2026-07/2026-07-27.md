# 2026-07-27

## Focus
- Fix CI pipeline after search rewrite

## Log

### Sessions 12:31 (el-mundo-ha-vivido-equivocado — 1 session)
- Fixed CI failures introduced by search rewrite:
  - Added `extractions/setup-just@v2` to install `just` in GH Actions runner
  - Fixed artifact checks: `core.mjs` → `core.js`, added `search.mjs` check
  - Updated justfile: `cp src/search.js dist/search.mjs` instead of stale squint cache
  - Updated `watch` target to output `core.js` (was `core.mjs`)
  - Added `npx playwright install chromium` step for smoke tests
- CI now passes (`"conclusion": "success"`)
- **Next:** P2 tickets — timezone bug (hi8), template escaping (pfb), org syntax (ae1)

### Sessions 20:57 (incitaciones — 1 session)
- Evaluated distribution options for incitaciones skills (npx vs pixi vs pi native)
- Created `scripts/cli.mjs` — cross-tool CLI for `npx incitaciones install/list/info`
- Updated `package.json` with `bin`, `files`, repository metadata, `publishConfig`
- Published `incitaciones@0.1.0` to npm (60 skills, 318 files, 263 kB)
- Set up CI/CD: `.github/workflows/npm-publish.yml` auto-publishes on `v*` tags
- Configured `NPM_TOKEN` as GitHub secret
- Published `incitaciones@0.2.0` via CI on tag push — pipeline verified working
- Merged `nucleus` branch into main (clean merge)
- Updated documentation: AGENTS.md (npm install, CI/CD), README.md (badges, publishing workflow, top 10 skills table)
- Scanned all 621 pi sessions across 43 repos for skill usage analysis
- Created `content/research-finding-skill-usage-analysis.md` with full adoption data
- **Arc:** First npm release of the skills repository — now distributable via `pi install npm:incitaciones` or `npx incitaciones install`
- **Next:** Promote newer skills (ui-align, guided-review, project-status-report) with better docs/examples

## Tasks created

## Decisions

